# 2026-08-15 生产候选 Schema 收敛与应用恢复证据

版本：`v0.1.377`

结论：指定的生产恢复候选存在已撤回分支遗留结构；本次已在不改写源候选的前提下归档历史数据、生成零漂移收敛候选，并通过隔离应用检查。但该恢复点已超过 24 小时，RPO 门禁正确失败，因此不允许作为生产切换点。

## 1. 输入与保护

- 源候选数据库：2,375,680 bytes，SHA-256 `7a16075992b5f0f4d0b5f3711e7d2b0fb0fa4541f2b31ca62a4122a138479845`。
- 业务数量只以匿名统计记录：45 个物料、62 条库存、1 张生产单、83 条有效附件记录和 252 个附件文件。
- 审计、收敛和应用演练开始与结束均校验源候选 SHA-256，整个过程没有改写源文件。
- 运行时 JSON 证据和数据候选位于 `.runtime/production-audit/` 与 `.runtime/drills/`，由 `.gitignore` 保护，不进入代码仓库。

## 2. 审计发现

| 项目 | 结果 |
| --- | --- |
| 目标主线迁移 | 82 个 |
| 待应用主线迁移 | 8 个，在隔离副本上全部成功 |
| 主线之外已应用迁移 | 6 个已撤回分支迁移 |
| Schema 额外对象 | 72 个：17 张表、52 个索引及 3 个核心表的历史额外列 |
| 缺失对象 | 0 |
| 含数据退役结构 | 17 张额外表中 16 张为空，`ManufacturingConfig` 有 1 行 |
| 分类 | `DATA_BEARING_REMOVED_SCHEMA` |
| 建议 | `STOP_ARCHIVE_AND_APPROVE_DATA_DISPOSITION` |

完整 diff SQL 的 SHA-256 为 `f8f90c927d22cb8e750fa3a3cb3173195e0a4fc54dc71634b14ff859b5868786`。这一结论意味着不能只说“迁移成功”：迁移记录全部应用后，物理结构仍可能与主线基线不一致。

## 3. 非覆盖收敛候选

| 项目 | 结果 |
| --- | --- |
| 退役结构归档 | 17 张表、1 行数据；SHA-256 `11c99568e1c19b6647b9eaaac5ddd3e1a045d593ed7f03f6771067b258095bac` |
| 收敛候选 | SHA-256 `cabdaf16f8176e926b6b5d2ce76f31239cdfb0332bf50ea5f84bd901971acad5` |
| SQLite 检查 | `quick_check=ok`、`integrity_check=ok`、外键错误 0 |
| 后置审计 | `NO_SCHEMA_DRIFT` |
| 源候选 | SHA-256 不变 |

归档并不授权丢弃那 1 行配置。在生产切换前，必须由业务负责人确认该数据已废弃、需转录到新模型，或需继续保留。

## 4. 收敛后应用演练

- 8 个待应用迁移、SQLite 完整性、全附件 SHA-256、readiness、隔离临时管理员登录、物料/库存/生产单接口和附件原文件接口全部 `PASS`。
- 临时管理员已清理；源候选和源附件未改写。
- 完整应用 RTO 为 2.287 秒，达到 60 分钟目标。
- 报告沿用原备份时间 `2026-08-13T11:55:00+08:00`，RPO 为 168,774.166 秒，超过 24 小时目标，整体状态正确为 `APPLICATION_FAIL`。

## 5. 未关闭的生产门禁

1. 从当前在线生产停止写入后创建新的数据库与附件一致备份，将 RPO 降到 24 小时内。
2. 对最新备份重新执行 Schema 审计、收敛候选和应用演练；不得重用本次旧报告。
3. 业务负责人签字确认退役数据处置和 Product→Material 最新映射。
4. 在真实 Coolify 维护窗口保留旧挂载、切换新候选，再完成公网健康、登录和关键业务抽查。
