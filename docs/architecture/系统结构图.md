# MES-lite 系统结构图

状态：当前代码与部署事实
事实基线：`v0.1.434` / 2026-08-22

本文从系统外部边界、运行部署、应用分层和领域模块四个层面说明 MES-lite。功能是否完备请结合[系统功能全景与完备性审查](./系统功能全景与完备性审查.md)，业务怎样流动请结合[当前功能流程与泳道](./MES当前功能流程与泳道.md)。

## 1. 系统上下文

```mermaid
flowchart LR
  subgraph Roles["工厂与协作角色"]
    Sales["销售"]
    Planner["计划 / 工程"]
    Warehouse["仓库"]
    Production["生产"]
    Quality["质量"]
    Maintenance["设备 / 维修"]
    Admin["管理员 / 运维"]
  end

  Mes["MES-lite<br/>单厂统一 Web 业务系统"]
  Supplier["供应商"]
  Customer["客户"]
  Ai["可选 OpenAI 兼容 AI 服务<br/>只读工具白名单"]
  Wechat["可选微信网页登录"]
  Oss["单一 HTTPS/OSS 下载点<br/>SOP / 视频 / 离线文件"]

  Sales --> Mes
  Planner --> Mes
  Warehouse --> Mes
  Production --> Mes
  Quality --> Mes
  Maintenance --> Mes
  Admin --> Mes
  Supplier -->|"来料、批号、价格"| Mes
  Mes -->|"订单、发货、退货追溯"| Customer
  Mes -."最小化只读查询".-> Ai
  Wechat -."身份回调".-> Mes
  Mes -."读取版本化媒体清单".-> Oss
```

当前边界是单厂、单实例、统一数据库。MES 是主体，MRP/ERP 是有限协同能力和预留入口，不是三套独立应用。桌面端和移动端若后续发布，只能作为连接同一 Web/API 的薄壳。

## 2. 运行与部署架构

```mermaid
flowchart TB
  Browser["浏览器 / 未来平台薄壳"] -->|"HTTPS"| Proxy["Coolify 反向代理与健康检查"]
  Proxy --> App["单个 MES-lite 容器<br/>Next.js standalone"]

  subgraph Container["应用容器"]
    App --> Middleware["Middleware<br/>会话 / Origin 边界"]
    Middleware --> UI["Server/Client UI + 公共应用壳"]
    Middleware --> Route["162 个 Route Handler<br/>鉴权 / Schema / 审计 / 响应"]
    Route --> Services["17 个模块的领域服务<br/>状态机 / 事务 / 查询"]
    Services --> Prisma["Prisma Client"]
  end

  Prisma --> Db["/app/data/mes_lite.db<br/>SQLite 持久目录"]
  Services --> Uploads["/app/public/uploads<br/>附件持久目录"]
  Backup["迁移前备份 / 定时备份 / 恢复演练"] --> Backups["/app/backups<br/>备份持久目录"]
  Db --> Backup
  Uploads --> Backup
  Services -.-> Oss["SOP / 视频外部 HTTPS 地址"]
  Services -.-> Ai["可选 AI 提供方"]
```

`/app/data`、`/app/public/uploads` 和 `/app/backups` 必须在 Coolify 中映射到明确、可识别和可备份的持久目录。镜像重建不能成为业务数据持久化机制；外部 SOP/视频下载点只保存发布媒体，不替代数据库与附件备份。

## 3. 领域模块与依赖方向

```mermaid
flowchart TB
  subgraph Experience["入口与体验"]
    Workspace["workspace<br/>工作台与聚合"]
    Sop["sop<br/>快捷/完整帮助与媒体关系"]
    System["system-settings<br/>显示、导航、文件预览、AI 设置"]
  end

  subgraph Master["主档与工程基础"]
    Materials["materials<br/>物料"]
    Bom["bom<br/>BOM 生命周期"]
    Documents["documents<br/>受控作业文件、分类字段与批量元数据"]
    Configuration["configuration<br/>公共业务配置"]
    Equipment["equipment<br/>工作中心、设备、点检、维保"]
  end

  subgraph Transaction["业务事务"]
    Receiving["receiving<br/>来料"]
    Production["production<br/>订单、派工、实绩、转移"]
    Quality["quality<br/>标准、任务、判定、处置"]
    Sales["sales<br/>订单、发货、退货"]
    Inventory["inventory<br/>批次、库存、成本、流水"]
  end

  subgraph Platform["平台支撑"]
    Identity["identity-access<br/>身份、权限、数据范围"]
    Attachments["attachments<br/>附件生命周期"]
    BusinessDocs["business-documents<br/>业务 PDF 与归档"]
    Tools["operations-tools<br/>数据、审计、备份与专项工具"]
  end

  Materials --> Bom
  Materials --> Receiving
  Materials --> Production
  Materials --> Sales
  Bom --> Production
  Documents --> Production
  Equipment --> Production
  Receiving --> Inventory
  Production --> Inventory
  Production --> Quality
  Receiving --> Quality
  Sales --> Inventory
  Sales --> Quality
  Quality --> Inventory
  Inventory --> Workspace
  Production --> Workspace
  Quality --> Workspace
  Equipment --> Workspace

  Identity -."约束所有读取与命令".-> Production
  Identity -.-> Receiving
  Identity -.-> Quality
  Identity -.-> Sales
  Identity -.-> Materials
  Attachments -."复用".-> Production
  Attachments -.-> Quality
  Attachments -.-> Materials
  BusinessDocs -."单据投影".-> Receiving
  BusinessDocs -.-> Sales
  Sop -."按 pageKey/流程关联".-> Workspace
  Sop -.-> Production
  Sop -.-> Quality
  Tools -."受控维护".-> Inventory
  System -."只改变显示与安全配置".-> Workspace
  Configuration -."提供公共主档选项".-> Materials
  Configuration -.-> Receiving
```

箭头表示业务事实或能力依赖，不表示允许任意跨模块导入。跨模块调用必须经模块公开入口；Route Handler 不拥有领域规则，UI 不直接请求数据库或复制事务逻辑。

## 4. 应用分层总体结构

```mermaid
flowchart TB
  subgraph Client["客户端"]
    Web["响应式 Web"]
    Desktop["未来桌面薄壳"]
    Mobile["未来移动薄壳"]
  end

  subgraph Frontend["Next.js 前端与应用壳"]
    Shell["导航、权限、页面注册、工作区"]
    Toolbar["公共工具栏、搜索、视图、页内选项"]
    Presentation["公共页面形态：弹窗、主显示区"]
    Resource["资源模板：列表、卡片、详情、编辑、附件"]
  end

  subgraph Domain["业务域"]
    MES["MES 核心：生产、实绩、产出批次、质量、库存状态、库位、设备、文档"]
    MRP["有限 MRP：BOM、生产订单、用量与需求基础"]
    ERP["有限 ERP：客户、供应商、销售订单、发货、退货"]
    Identity["共享身份权限：账号、权限组、人员授权"]
    Operations["运维工具：数据检查、归档、审计"]
  end

  subgraph Server["Next.js Route Handlers 与领域服务"]
    API["HTTP 适配：权限、请求校验、审计、响应"]
    Services["领域服务：事务、批次、质量、库存状态、工艺、单位、成本、订单状态、附件"]
    Audit["审计、归档与数据一致性检查"]
  end

  subgraph Data["数据与文件"]
    Prisma["Prisma ORM"]
    DB["当前 SQLite；PostgreSQL 为暂停的长期候选"]
    Storage["原始附件、预览文件、业务 PDF"]
  end

  Web --> Shell
  Desktop --> Web
  Mobile --> Web
  Shell --> Toolbar
  Shell --> Presentation
  Presentation --> Resource
  Resource --> MES
  Resource --> MRP
  Resource --> ERP
  Resource --> Identity
  Resource --> Operations
  MES --> API
  MRP --> API
  ERP --> API
  Identity --> API
  Operations --> API
  API --> Services
  API --> Audit
  Services --> Prisma
  Audit --> Prisma
  Prisma --> DB
  Services --> Storage
```

## 5. 页面模块化骨架

```mermaid
flowchart LR
  Registry["Page Registry\n页面能力与打开模式"] --> Boundary["PageModuleBoundary"]
  Boundary --> Shell["HomeApp 主显示区"]
  Shell --> Toolbar["公共顶部工具栏"]
  Shell --> Gateway["模块公开出口 index.ts"]
  Gateway --> Page["领域页面 / SectionPage"]
  Page --> Resource["公共资源组件"]
  Page --> Dialog["ModalDialog"]
  Page --> Client["模块 client"]
  Client --> API["Next.js API 适配层"]
  API --> Service["模块 server / application"]
  Service --> Prisma["Prisma 与存储适配"]
  Dialog --> Overlay["ModalOverlay"]
  Dialog --> Fullscreen["同组件可逆全屏"]
```

页面只声明能力和业务内容。搜索、视图、页内选项、弹窗标题区、可逆全屏、附件管理等行为必须复用公共模块；业务页面不得并行实现“主页面打开”形态，专用文件查看器继续复用文件模块的全屏预览。仅复杂 BOM 工作区、仪表盘等页面级场景允许注册为例外。

### 5.1 参数化往来单位切片

```mermaid
flowchart LR
  SupplierRoute["供应商 Route\nsupplier 声明"] --> HTTP["配置领域 HTTP 适配器\n权限、Schema、审计、响应"]
  CustomerRoute["客户 Route\ncustomer 声明"] --> HTTP
  HTTP --> Definition["资源定义\n名称、审计类型、排序、唯一性"]
  HTTP --> Service["往来单位服务\n搜索、新增、更新、归档"]
  Service --> Supplier["Supplier Prisma 适配"]
  Service --> Customer["Customer Prisma 适配"]
  Supplier --> DB["SQLite / PostgreSQL"]
  Customer --> DB
```

参数化只覆盖字段结构和生命周期真正同构的供应商、客户资料。员工、库位、单位和工作中心仍有账号绑定、默认库位、单位换算或设备关联等独立规则，不能为了减少文件数强行接入同一万能 CRUD。

### 5.2 库位专用领域服务

```mermaid
flowchart LR
  Page["公共 ResourcePage\n库位配置"] --> Client["配置领域 client"]
  Client --> API["库位 HTTP 适配\n登录、权限、Schema、审计"]
  API --> Query["查询服务\n状态与余额汇总"]
  API --> Command["命令服务\n默认、更新、归档、恢复"]
  Command --> Rules["纯规则\n编码、默认状态、库存阻断"]
  Query --> Prisma["Prisma"]
  Command --> Prisma
  Prisma --> DB["InventoryLocation\nStockLocationBalance\n待处理业务单据"]
```

库位页面仍使用公共资源骨架，但服务端保持专用领域规则；它不复用客商 CRUD，因为默认库位和库存/业务引用约束无法由普通资料配置安全表达。

### 5.3 库存流水查询切片

```mermaid
flowchart LR
  Page["库存流水页面\n卡片 / 列表 / 分页"] --> Toolbar["公共工具栏\n智能搜索 / 字段搜索 / 视图"]
  Page --> Client["inventory client"]
  Client --> API["只读 HTTP 适配\nstocks:read"]
  API --> Query["库存流水查询服务\n分词、条件、分页、选项"]
  Query --> Log["StockLog"]
  Query --> Stock["Stock + Material / Product"]
  Query --> Location["InventoryLocation"]
```

库存流水页面只提供可验证的查账入口，既有库存过账仍由来料、生产、销售、流程转移和库存调整领域服务写入同一个 `StockLog`，不能从页面建立第二套账。精确冲销通过 `sourceMovementId` / `reversalMovementId` 双向关联并由服务与数据库共同保护；部分退货等合法一对多回流只保留一般来源关联。

#### 生产日报 BOM 快捷转换与独立库存盘点切片

```mermaid
flowchart LR
  Page["生产菜单 / 生产日报\nBOM 快捷转换"] --> API["daily-production-shortcut\nstocks.update"]
  API --> Production["生产领域事务\n正式 BOM + 实际投入/产出"]
  Production --> Input["投入 FIFO 扣减"]
  Production --> Output["主产出入库 + 成本结转"]
  Output --> Disposition{"产出处置"}
  Disposition -->|直接入库| Available["可用库存"]
  Disposition -->|后续质检| Quarantine["待检批次 + 质量任务"]
  Production --> Report["DailyProductionReport + 审计"]
  CountPage["库存菜单 / 库存盘点"] --> Count["daily-count\n账实差异校准"]
  Production -.不创建.-> Order["生产订单 / 派工 / 报工"]
```

生产日报允许管理人员不填写生产人员、设备和工序，按可选的已发布 BOM 预填或直接登记实际投入与多个实际产出，完成库存转换和成本记录。产出可直接进入可用库存，也可进入待检批次并生成质量任务；需要人员、设备、作业文件和完整投入产出谱系时仍走生产订单实绩。库存盘点是库存菜单下的独立页面，只写库存差异事实。

### 5.4 设备与工作中心领域切片

```mermaid
flowchart LR
  EquipmentPage["设备台账 ResourcePage"] --> Client["设备领域 client"]
  EventDialog["运行事件命令与时间线"] --> Client
  ConfigEntry["业务配置 / 工作中心入口"] --> Public["modules/equipment/index.ts"]
  Public --> CenterPage["工作中心 ResourcePage"]
  CenterPage --> Client
  Client --> EquipmentAPI["设备 HTTP 适配层"]
  Client --> CenterAPI["工作中心 HTTP 适配层"]
  EquipmentAPI --> EquipmentService["设备查询与命令服务"]
  EquipmentAPI --> EventService["设备事件状态机服务"]
  CenterAPI --> CenterService["工作中心查询与命令服务"]
  EquipmentService --> Rules["编码、有效归属、归档规则"]
  EventService --> Rules
  CenterService --> Rules
  EquipmentService --> Prisma["Prisma Transaction"]
  EventService --> Prisma
  CenterService --> Prisma
  Prisma --> Models["WorkCenter / Equipment / EquipmentEvent / WorkInstruction"]
```

菜单入口不等于代码所有权。工作中心虽然从业务配置菜单进入，但它定义设备能力区域并约束设备归属，因此其 UI、契约、搜索、client、规则和事务均由设备领域拥有；配置模块只负责 section 分派。设备基础资料只维护静态字段，运行状态由 `EquipmentEvent` 状态机与设备快照在同一事务更新。

### 5.5 生产产出批次与质量状态切片

```mermaid
flowchart LR
  Context["人员 + 实际设备 + 作业文件版本"] --> Actual["确认生产实绩"]
  Actual --> Production["生产领域事务"]
  Production --> Lot["InventoryLot + Balance + Transaction"]
  Production --> Inspection["QualityInspection 待检任务"]
  Production --> Quarantine["Stock / Location / CostLayer\nQUARANTINE"]
  OrderDetail["生产订单详情 / 质量任务\n批次质量卡片"] --> API["质量 HTTP 适配\n查看 / 判定 / 处置 / 授权放行"]
  API --> Quality["modules/quality\n判定事务"]
  Quality --> Inventory["modules/inventory\n状态转换"]
  Inventory --> Available["AVAILABLE"]
  Inventory --> Hold["HOLD"]
  Inventory --> Log["InventoryLotTransaction + StockLog"]
```

该切片保持生产、质量和库存的代码所有权：生产负责何时产生批次，质量负责检验判定与处置，库存负责状态余额、成本层和追溯读取。`v0.1.357` 增加跨工单质量任务入口，`v0.1.358` 已增加只读的可搜索批次全景。

### 5.6 来料到生产批次谱系切片

```mermaid
flowchart LR
  Receiving["来料收货"] --> RawLot["InventoryLot<br/>内部批号 + 供应商批号"]
  RawLot --> Transfer["流程转移<br/>同步批次库位余额"]
  Transfer --> Allocation["InventoryLotAllocation<br/>投入 FIFO 分配"]
  Allocation --> Genealogy["InventoryLotGenealogy<br/>父批次 → 子批次"]
  Genealogy --> OutputLot["产出批次 + 待检"]
  OutputLot --> ShipmentAllocation["ShipmentLotAllocation<br/>客户发货批次"]
  ShipmentAllocation --> ReturnAllocation["ReturnLotAllocation<br/>客户退货回流"]
  ReturnAllocation --> ReturnLot["退货独立待检批次"]
  Trace["来料/生产/发货/退货追溯弹窗"] --> Query["GET /api/inventory-lots/:id/trace<br/>stocks.read"]
  Query --> RawLot
  Query --> Genealogy
  Query --> OutputLot
  Query --> ShipmentAllocation
  Query --> ReturnLot
```

收货、生产和销售继续拥有自己的单据状态机，批次余额、分配、谱系与追溯查询归 `modules/inventory`。追溯 UI 复用公共 `ModalDialog` 和 `AppButton`，显示相邻生产节点和客户履约节点并允许继续导航；质量卡由 `modules/quality` 在生产和退货详情共同复用。

### 5.7 BOM 快捷生产日报与历史兼容切片

```mermaid
flowchart LR
  QuickPage["生产日报 BOM 快捷入口"] --> QuickAPI["专用薄 HTTP 适配层\n权限、Schema、数据范围"]
  QuickAPI --> QuickTx["快捷事务服务\n创建 + 确认 + 审计"]
  LegacyCaller["历史兼容调用方"] --> API["4 条薄 HTTP 适配层\n权限、Schema、审计、响应"]
  API --> Query["查询服务\n日报、BOM、物料、员工"]
  API --> Command["草稿命令服务\n日期编号、BOM 与人员快照"]
  API --> Status["状态事务服务\n确认过账、成本结转、冲销"]
  Command --> Rules["纯领域规则\n日期、最大序号、状态约束"]
  Status --> Inventory["共享库存服务"]
  QuickTx --> Status
  Query --> Prisma["Prisma"]
  Command --> Prisma
  Status --> Prisma
  Prisma --> LegacyModels["DailyProductionReport\nConsumption / Employee"]
```

旧的大型通用日报页面仍不可达，4 条旧 API 暂留用于解释和处理服务器历史数据，旧创建接口继续返回 `410`。`v0.1.433` 只新增专用 BOM 快捷入口：它复用同一状态与库存服务，在一个事务中创建并确认日报，但不创建订单、派工或报工；选择待检时会复用统一质量任务。需要完整人员、设备、作业文件和谱系追溯时仍使用生产订单实绩。

## 6. 功能域定位

| 域 | 当前定位 | 本阶段包含 | 本阶段不做 |
| --- | --- | --- | --- |
| MES | 主体 | 生产订单、带人员/实际设备/作业文件版本快照的生产实绩、BOM 快捷投入/主产出过账、来料/产出/退货内部批次、投入与发货 FIFO 分配、父子谱系、客户去向、退货回流、质量处置、可用/待检/冻结库存、库位、流程转移、设备、作业文档 | 序列号、高级 APS、自动设备采集、节拍和 OEE |
| MRP | 有限支持 | BOM、生产用量、基础计划数据 | 自动净需求、完整采购建议、跨组织计划 |
| ERP | 有限支持 | 客户、供应商、销售订单、销售价、发货、退货 | 财务总账、应收应付、税务、自动订单编排 |

前端只有一个统一 MES 工作台，按现场任务和功能区组织菜单。左上角 MES-lite/MRP/ERP 是可改名、可控制预留项显隐的模块按钮：MES-lite 点击后返回仪表盘首页，MRP/ERP 不执行工作区切换；系统版本显示在左侧导航底部。MES、MRP-lite、ERP-lite 仅表示领域责任，不复制页面或数据库；跨域关系通过明确字段和用户动作建立，不做隐式级联。
