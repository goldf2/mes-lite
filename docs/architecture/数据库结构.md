# MES-lite 数据库结构

本文以 `prisma/schema.prisma` 为唯一实现来源，事实基线为 `v0.1.277`。当前运行库为 SQLite，模型设计通过 Prisma 保持向 PostgreSQL 迁移的可能性；`docs/minierp/data-model.md` 中的租户字段属于目标草案，不代表当前已实现。

## 1. 核心实体关系

```mermaid
erDiagram
  Customer ||--o{ Material : "归属客户"
  Customer ||--o{ SalesOrder : "下单"
  Customer ||--o{ Shipment : "收货方"
  Supplier ||--o{ MaterialIn : "供货"

  Material ||--o| Stock : "汇总库存"
  Stock ||--o{ StockLocationBalance : "库位余额"
  InventoryLocation ||--o{ StockLocationBalance : "存放"
  Stock ||--o{ StockLog : "库存流水"
  InventoryLocation ||--o{ StockLog : "发生库位"

  BOM ||--o{ BOMItem : "投入"
  BOM ||--o{ BOMOutput : "产出"
  Material ||--o{ BOMItem : "投入物料"
  Material ||--o{ BOMOutput : "产出物料"
  BOM ||--o{ ProductionOrder : "生产方案"
  Material ||--o{ ProductionOrder : "目标物料"
  ProductionOrder ||--o{ ProductionOrderActual : "生产实绩"
  ProductionOrderActual ||--o{ ProductionOrderActualInput : "实际投入"
  ProductionOrderActual ||--o{ ProductionOrderActualOutput : "实际产出"

  Material ||--o{ MaterialIn : "来料"
  Material ||--o{ InventoryCostLayer : "成本层"
  MaterialIn ||--o{ InventoryCostLayer : "形成成本层"

  SalesOrder ||--|{ SalesOrderItem : "订单明细"
  Material ||--o{ SalesOrderItem : "销售物料"
  SalesOrder ||--o{ Shipment : "可选来源"
  SalesOrderItem ||--o{ Shipment : "可选来源明细"
  Material ||--o{ Shipment : "发货物料"
  InventoryLocation ||--o{ Shipment : "发货库位"
  Shipment ||--o{ ReturnOrder : "可选退货来源"

  DocumentCategory ||--o{ WorkInstruction : "分类"
  Material ||--o{ WorkInstruction : "可选关联"
  DocumentAttachment }o--|| WorkInstruction : "ownerType-ownerId"
  DocumentAttachment }o--|| SalesOrder : "ownerType-ownerId"
  DocumentAttachment }o--|| Shipment : "ownerType-ownerId"

  Operator ||--o{ AuditLog : "操作人快照"
```

`DocumentAttachment` 使用 `ownerType + ownerId` 形成通用多态关联，数据库不建立具体外键；服务端负责权限、归档和目标存在性校验。

## 2. 销售价格字段

| 模型 | 字段 | 含义 |
| --- | --- | --- |
| `Material` | `defaultSalePrice` | 可空的默认销售价；用于新单据带入，不是成本 |
| `Material` | `salesCurrency` | 默认销售币种，当前界面使用 `CNY` |
| `SalesOrder` | `currency` | 订单币种快照 |
| `SalesOrderItem` | `unitPrice / totalAmount` | 订单明细成交单价与金额快照 |
| `SalesOrderItem` | `priceSource` | `MATERIAL_DEFAULT` 或 `MANUAL` |
| `SalesOrderItem` | `defaultSalePriceSnapshot` | 下单时物料默认价，用于解释价格来源 |
| `SalesOrderItem` | `priceAdjustedAt / By / Reason` | 后续手工调价审计信息 |

销售价与成本严格分离：`Material` 不增加成本字段。库存成本由 `InventoryCostLayer`、`CostLayerConsumption`、`BomCostRun`、生产实际投入/产出及相关成本记录维护。

## 3. 销售订单与发货关系

```mermaid
erDiagram
  SalesOrder {
    string id PK
    string orderNo UK
    string customerId FK
    string status
    float totalAmount
    string currency
  }
  SalesOrderItem {
    string id PK
    string salesOrderId FK
    string materialId FK
    float qty
    float shippedQty
    float unitPrice
    string priceSource
  }
  Shipment {
    string id PK
    string shipmentNo UK
    string salesOrderId FK "nullable"
    string salesOrderItemId FK "nullable"
    string materialId FK
    string customerId FK
    string locationId FK
    float qty
    float unitPrice
    string status
  }
  SalesOrder ||--|{ SalesOrderItem : contains
  SalesOrder ||--o{ Shipment : optional_source
  SalesOrderItem ||--o{ Shipment : optional_source_line
```

- 独立发货：`salesOrderId` 与 `salesOrderItemId` 均为空，客户、物料、库位和价格由发货单明确保存。
- 关联发货：两个来源字段有值，创建时校验订单状态和剩余量，并冻结客户、物料和订单单价。
- 发货确认才影响库存；创建销售订单本身不扣减库存。
- 已产生发货记录后销售价格锁定，不自动回写既有发货单金额。

## 4. 数据一致性边界

1. `Stock` 是物料总量，`StockLocationBalance` 是分库位量；确认类业务在同一事务内更新两者和 `StockLog`。
2. 销售订单、发货、退货彼此独立建单；只有显式来源关系才参与数量或状态计算。
3. 物料默认销售价只影响新快照，不批量改写历史订单。
4. 业务记录使用软归档；审计日志保存操作人、前后数据和原因。
5. 原始附件永久保留策略、预览衍生文件和业务 PDF 由文件存储层管理，数据库只保存元数据和路径。
