# MES-lite MES / MRP / ERP 功能矩阵

状态：架构基线，后续功能立项与页面归属必须遵循
日期：2026-08-08

HTML 展示版：[打开功能矩阵](../../public/mes-mrp-erp-function-matrix.html)

## 1. 决策摘要

MES-lite 采用“**MES 为核心、MRP 与 ERP 有限支持**”的产品边界：

- **MES 核心**负责把生产任务变成可执行、可记录、可追溯的现场事实，是系统必须保持完整的主能力。
- **MRP-lite**负责从需求、BOM 和库存推导缺料及建议，不建设复杂 APS、全自动采购或多工厂全局优化。
- **ERP-lite**只补齐独立运行所需的客户、供应商、销售、来料计价、发货和退货，不扩展总账、应收应付、税务、发票、资金和薪资。
- **共享平台**统一提供账号权限、附件、搜索、审计、AI、显示设置和数据维护，不在三个工作域重复开发。
- 有外部 ERP 时，由 ERP 负责商业主数据和财务口径，MES-lite 通过接口接收任务、回传生产实绩；没有外部 ERP 时，可启用 MRP-lite 和 ERP-lite 形成轻量闭环。

三个工作域首先是**逻辑分层和可开关模块**，不是三套前端、三套数据库或三个独立系统。当前继续采用模块化单体，避免重复物料、库存、附件、权限和页面框架。

## 2. 标记说明

### 2.1 责任标记

| 标记 | 含义 |
| --- | --- |
| **主** | 该工作域是能力的业务负责人和事实来源 |
| **用** | 使用该能力或向负责人提供输入，不拥有最终口径 |
| **接** | 主要通过接口同步，不在本系统重复建设 |
| — | 不属于该工作域 |

### 2.2 当前状态

| 状态 | 含义 |
| --- | --- |
| **已有** | 已有可用页面或业务流程 |
| **部分** | 已有基础数据或操作，但尚未形成完整业务闭环 |
| **规划** | 属于目标边界，尚待实现 |
| **不建设** | 明确排除在 MES-lite 产品范围外，交由外部系统 |

## 3. 总体功能矩阵

### 3.1 主数据、工程与组织

| 功能 | MES | MRP | ERP | 当前状态 / 页面 | MES-lite 目标与默认模块 |
| --- | :---: | :---: | :---: | --- | --- |
| 企业资料与业务编号规则 | 用 | 用 | **主** | 已有：企业与业务规则 | 独立运行时由 ERP-lite 提供；集成模式由外部 ERP 同步 |
| 账号、权限组与人员权限 | **主** | 用 | 用 | 已有：人员管理、人员权限、组权限 | 共享平台必选，三个工作域共用同一权限模型 |
| 员工基础资料 | **主** | — | 用 | 已有：员工资料 | MES 保留现场人员身份；薪酬和人事档案不建设 |
| 客户资料 | 用 | 用 | **主** | 已有：客户资料 | ERP-lite 可选；集成模式只读同步 |
| 供应商资料 | 用 | 用 | **主** | 已有：供应商资料 | ERP-lite 可选；来料和缺料建议只引用统一供应商标识 |
| 物料与 SKU | **主** | 用 | 用 | 已有：物料管理 | 共享主数据，只保留一个物料事实源，不创建 MES/ERP 两套物料 |
| 物料变体与规格组合 | **主** | 用 | 用 | 规划 | 作为标准 SKU 生成与选择能力；不得替代批次、质量或库存状态 |
| 单位与量纲换算 | **主** | 用 | 用 | 已有：单位配置 | 共享业务配置，库存、BOM、计划和单据统一换算规则 |
| BOM 与版本 | **主** | 用 | 用 | 已有：BOM 设置、BOM 全览 | MES 管工程结构和投产版本；MRP 使用有效版本展开需求 |
| 工艺模板与工艺路线 | **主** | 用 | — | 已有：加工工艺、物料路线 | MES 核心；MRP 仅使用工时、产能和提前期输入 |
| 工作中心 | **主** | 用 | — | 已有：工作中心 | MES 核心；作为派工、报工和有限产能计算基础 |
| 设备台账 | **主** | — | — | 已有：设备台账 | MES 核心；财务资产折旧不纳入设备台账 |
| 产品文档与文档分类 | **主** | 用 | 用 | 已有：产品文档、文档类别 | 共享附件与预览底座；作业指导和图纸由 MES 管理 |

### 3.2 需求、计划与物料需求

| 功能 | MES | MRP | ERP | 当前状态 / 页面 | MES-lite 目标与默认模块 |
| --- | :---: | :---: | :---: | --- | --- |
| 销售需求与销售订单 | 用 | 用 | **主** | 已有：销售订单 | ERP-lite 可选；集成模式从外部 ERP 接收已确认需求 |
| 生产需求与生产订单 | **主** | 用 | 用 | 已有：生产订单 | MES 核心，负责生产状态和实际完成量 |
| BOM 多层展开 | 用 | **主** | 用 | 部分：已有 BOM 结构与用量查询 | MRP-lite 必须形成按有效 BOM、损耗和单位换算的确定性展开 |
| 毛需求计算 | 用 | **主** | 用 | 规划 | MRP-lite 可选，由销售需求、生产需求或手工需求触发 |
| 净需求计算 | 用 | **主** | 用 | 规划 | 扣减可用库存、已预留、在途和已下达供应 |
| 库存预留与可用量 | **主** | 用 | 用 | 部分：已有库存与销售占用 | MES 管实物与预留事实；MRP 只消费统一可用量口径 |
| 缺料分析 | 用 | **主** | 用 | 规划 | MRP-lite 首要能力，输出缺料、需求时间和来源追溯 |
| 建议生产单 | 用 | **主** | 用 | 规划 | 仅生成建议，必须由用户确认后创建 MES 生产订单 |
| 建议采购单 | 用 | **主** | 用 | 规划 | 仅生成建议；正式采购单由 ERP-lite 或外部 ERP 创建 |
| 提前期与齐套检查 | 用 | **主** | 用 | 规划 | MRP-lite 支持基础固定提前期和订单齐套，不做高级概率预测 |
| 产能粗算 | 用 | **主** | — | 规划 | 可依据工艺路线和工作中心做负荷提示，不承诺最优排程 |
| 有限能力 APS 排程 | 用 | 接 | — | 不建设 | 交由专业 APS；MES-lite 只接收排程结果并执行 |
| 需求预测与 S&OP | 用 | 接 | **主** | 不建设 | 交由 ERP、SCM 或专业预测系统 |

### 3.3 生产执行、质量与设备

| 功能 | MES | MRP | ERP | 当前状态 / 页面 | MES-lite 目标与默认模块 |
| --- | :---: | :---: | :---: | --- | --- |
| 生产订单下达与状态流转 | **主** | 用 | 用 | 已有：生产订单 | MES 核心，外部系统只接收状态和完成结果 |
| 派工与任务领取 | **主** | — | — | 已有：派工 | MES 核心，按人员、设备或工作中心执行 |
| 生产报工与实际数量 | **主** | 用 | 用 | 部分：已有计划与实际产量记录 | 补齐工序级开工、完工、暂停、人工和机时 |
| 在制品与流程转移 | **主** | 用 | — | 已有：流程转移 | MES 核心，形成节点、库位和责任人可追溯链 |
| 投料、退料与产出入库 | **主** | 用 | 用 | 部分：投料 FIFO 与产出谱系已实现 | 生产确认/冲销已与批次余额一致；独立退料单待补 |
| 生产、退货与来料质量检验 | **主** | — | 用 | 已有：标准版本、按标准启用的来料送检、自动抽样、逐项判定、附件与趋势 | 已记录标准快照、抽样、项目结果、责任人、放行/冻结和后续轮次；SPC/过程能力与多级审批待补 |
| 不合格、返工、报废 | **主** | 用 | 用 | 已有：复检、返工、报废、让步、解冻 | 每个处置动作写独立记录，并同步批次余额、库存流水、成本层与审计人 |
| 批次、序列号与正反向追溯 | **主** | 用 | 用 | 批次级：供应商→来料→生产→发货客户→退货回流已贯通，并可按批号、客商和业务单号搜索全景 | MES 核心；逐件序列号尚未实现 |
| 作业指导与图纸下发 | **主** | — | — | 已有：产品文档 | MES 核心，按物料、工艺、设备和版本关联 |
| 设备状态与点检维护 | **主** | 用 | — | 部分：已有设备台账 | 补齐状态事件、点检和保养；资产折旧交由 ERP |
| 生产实际成本 | **主** | 用 | 用 | 部分：锯切成本、工艺成本参数 | MES 记录实际材料、人工、机时；ERP 负责财务结转 |
| 扫码、计数与标签打印 | **主** | — | 用 | 已有：硬件工具 | 共享现场工具，由 MES 流程调用，不成为独立业务账本 |

### 3.4 库存、来料与物流

| 功能 | MES | MRP | ERP | 当前状态 / 页面 | MES-lite 目标与默认模块 |
| --- | :---: | :---: | :---: | --- | --- |
| 库位与用途配置 | **主** | 用 | 用 | 已有：库位配置 | 生产库存必选，统一服务来料、在制品、成品和发货 |
| 实物库存数量 | **主** | 用 | 用 | 已有：库存管理 | MES/WMS 口径；任何商业模块不得维护第二套数量 |
| 库存流水查询 | **主** | 用 | 用 | 已有：库存流水 | 查询数量、核算量、成本、库位、来源和操作人；所有模块只写同一流水事实源 |
| 批次、质量和处置状态库存 | **主** | 用 | 用 | 已有：来料/产出/退货批次与五态库存 | 已有可用/待检/冻结/返工/报废、发货分配、退货回流和状态流水；逐件序列号待补 |
| 供应商来料登记 | **主** | 用 | 用 | 已有：来料管理 + 内部批次 | 实物接收属于 MES/WMS；内部批号与供应商批号并存，价格和税务属于 ERP |
| 来料检验 | **主** | — | 用 | 已有：按已发布标准自动隔离、建任务、判定与红冲联动 | 无标准物料保持直接可用；让步接收复用质量处置，退供应商仍按真实业务需求补齐 |
| 领料、退料、生产入库 | **主** | 用 | 用 | 部分 | MES 核心，统一走库存事务，不允许直接改余额 |
| 库位调拨与流程转移 | **主** | 用 | 用 | 已有：流程转移 | MES/WMS 负责实物流转，ERP 只接收汇总或凭证 |
| 发货执行 | 用 | — | **主** | 已有：发货管理 | 独立模式由 ERP-lite 提供；集成模式可保留仓储执行界面 |
| 销售退货与返库 | 用 | — | **主** | 已有：退货管理 | ERP-lite 可选；返库后的检验和库存状态由 MES 接管 |
| 库存计价与成本层 | 用 | — | **主** | 部分：库存成本 | 独立模式只保留基础参考成本；财务库存估值以 ERP 为准 |
| 仓储波次、路线和自动化 WMS | 用 | — | 接 | 不建设 | 交由专业 WMS，MES-lite 保留库存与生产接口 |

### 3.5 商业、财务与企业管理

| 功能 | MES | MRP | ERP | 当前状态 / 页面 | MES-lite 目标与默认模块 |
| --- | :---: | :---: | :---: | --- | --- |
| 客户与供应商商业资料 | 用 | 用 | **主** | 已有：客户资料、供应商资料 | ERP-lite 可选；集成模式由外部 ERP 主控 |
| 销售价、订单金额 | 用 | 用 | **主** | 已有：销售订单 | 仅用于独立运行的业务闭环，不进入财务总账 |
| 采购价与来料金额 | 用 | 用 | **主** | 已有：来料管理 | 独立模式保留参考计价；集成模式采用 ERP 口径 |
| 销售发货与退货单据 | 用 | — | **主** | 已有：发货、退货 | ERP-lite 可选，MES 只消费交付需求和反馈实绩 |
| 采购订单与供应商交期 | 用 | 用 | **主** | 规划 | 仅做 MRP 建议到 ERP 的接口；不优先建设完整采购模块 |
| 总账、会计凭证和财务报表 | — | — | 接 | 不建设 | 必须由外部 ERP 或财务系统负责 |
| 应收、应付、收付款 | — | — | 接 | 不建设 | 不进入 MES-lite 数据模型和页面 |
| 发票、税务与资金管理 | — | — | 接 | 不建设 | 不进入 MES-lite 数据模型和页面 |
| 薪资、人事和绩效管理 | 用 | — | 接 | 不建设 | MES 只记录生产人员和操作实绩 |
| CRM、营销与售后服务 | — | — | 接 | 不建设 | 由 CRM/ERP 负责，必要时只同步客户与需求 |

### 3.6 共享平台能力

| 功能 | MES | MRP | ERP | 当前状态 / 页面 | MES-lite 目标与默认模块 |
| --- | :---: | :---: | :---: | --- | --- |
| 公共页面框架、视图和搜索 | **主** | 用 | 用 | 已有：公共资源页、页面工具 | 所有工作域使用同一骨架，不允许页面各自重建 |
| 附件上传、下载与预览 | **主** | 用 | 用 | 已有：公共附件管理 | 共享平台必选，按资源类型与记录 ID 关联 |
| 单据 AI 识别与辅助填写 | **主** | 用 | 用 | 已有：六类核心单据 | 只建议和回填，不自动确认、过账或改变业务状态 |
| AI 服务配置 | **主** | 用 | 用 | 已有：AI 服务 | 系统设置，统一模型、密钥、权限和审计 |
| 显示、对比度与导航布局 | **主** | 用 | 用 | 已有：显示设置 | 系统设置，不进入业务配置和页内选项 |
| 当前页面二维码 | **主** | 用 | 用 | 已有：页面工具 | 共享工具，生成页面定位信息，不复制业务数据 |
| 归档与恢复 | **主** | 用 | 用 | 已有：归档记录 | 共享生命周期；正式业务单据优先冲销而非物理删除 |
| 操作审计 | **主** | 用 | 用 | 已有：操作记录 | 共享平台必选，覆盖配置、主数据和业务状态变化 |
| 数据一致性检查与维护 | **主** | 用 | 用 | 已有：数据工具 | 只允许可审计、可预检的受控修复 |

## 4. 当前页面的工作域归属

| 现有一级菜单 / 页面 | 默认工作域 | 模块开关 | 说明 |
| --- | --- | --- | --- |
| 工作台、全部功能 | 共享 | 必选 | 根据已启用模块组合指标和入口，不复制工作台 |
| 物料管理、BOM 设置 | MES | 必选 | MRP 和 ERP 通过业务关联引用同一物料与 BOM，不重复显示菜单入口 |
| BOM 全览 | MRP-lite | 可选 | 作为当前已有的结构展开与用量检查入口；后续承接净需求、缺料和齐套页面 |
| 生产订单、派工、流程转移 | MES | 必选 | 生产执行主链路 |
| 产品文档、设备台账 | MES | 必选 | 生产资源与现场指导 |
| 来料管理 | MES | 生产库存必选；商业字段可选 | 页面归 MES；供应商价格与金额仍按字段边界由 ERP-lite 负责 |
| 库存管理、库存流水 | MES | 生产库存必选；计价可选 | 页面归 MES；财务估值仍按字段边界由 ERP 负责 |
| 销售订单、发货、退货 | ERP-lite | 可选 | 集成模式可隐藏销售订单，按需要保留仓储发退货执行 |
| 供应商、客户 | ERP-lite | 可选 | 外部 ERP 为主时只读同步或隐藏维护入口 |
| 员工、库位、单位、工作中心、工艺、路线、文档类别 | MES 业务配置 | 必选 | 直接影响生产业务数据和流程 |
| 企业与业务规则 | ERP-lite | 必选 | 只保留一个菜单入口，内部仍按设置项细分业务所有权 |
| 显示设置、AI 服务 | 共享系统设置 | 必选 | 与业务数据无关，统一服务全部工作域 |
| 锯切成本、硬件工具 | MES 工具 | 按需 | 服务现场执行，不形成独立账本 |
| 归档、操作记录、数据工具 | 共享运维 | 必选 | 权限控制后供全域使用 |
| 账号与权限 | 共享身份平台 | 必选 | 不归入 ERP 人事模块 |

## 5. 边界页面的字段级规则

一个页面同时服务多个工作域时，仍只保留一套页面和数据模型，通过字段分区、权限和模块开关控制，不复制页面。

| 共享对象 / 页面 | MES 负责字段 | MRP 使用字段 | ERP 负责字段 |
| --- | --- | --- | --- |
| 物料 | 编码、名称、规格、生产单位、BOM、路线、质量和追溯属性 | 计划策略、损耗、提前期、安全库存 | 销售/采购单位、税务分类、商业价格 |
| 物料变体 | 变体维度、规格组合、可生产 SKU、工艺差异 | 变体级需求和库存可用量 | 变体级销售/采购编码和价格 |
| 来料单 | 实收数量、单位、批次、实测、检验、库位、附件 | 在途到货和可用日期 | 供应商、采购单、单价、税率、结算口径 |
| 库存 | 实物数量、库位、批次、质量状态、预留、流水 | 可用量、在途、需求占用 | 计价方法、成本层、财务库存价值 |
| 销售订单 | 可生产性反馈、生产关联、实际交付 | 独立需求、需求日期、优先级 | 客户、价格、金额、商业状态和交付承诺 |
| 生产订单 | 状态、工序、投料、产量、损耗、人工、机时 | 建议来源、需求追溯、计划日期 | 外部订单号、成本结转和完工回传标识 |
| 工作台 | 产量、在制、质量、设备和异常 | 缺料、齐套和建议 | 销售、采购和金额；启用 ERP-lite 时才显示 |

## 6. 两种部署模式的数据所有权

| 数据对象 | 独立运行模式 | 对接 ERP 模式 |
| --- | --- | --- |
| 物料、单位和 BOM | MES-lite 主控 | 按企业约定：物料商业字段由 ERP 主控，工程字段由 MES-lite 主控 |
| 客户、供应商 | ERP-lite 主控 | 外部 ERP 主控，MES-lite 只读引用 |
| 销售订单 | ERP-lite 主控 | 外部 ERP 主控，MES-lite 接收已确认需求 |
| MRP 计算与建议 | MRP-lite 主控 | 可关闭；或仅做现场缺料校验，不回写正式采购单 |
| 生产订单、派工和报工 | MES-lite 主控 | MES-lite 主控，接收外部来源并回传状态与实际 |
| 实物库存和批次 | MES-lite 主控 | MES-lite/WMS 主控，向 ERP 回传事务或余额 |
| 库存财务价值 | 基础参考口径 | 外部 ERP 主控，MES-lite 不反向覆盖 |
| 发货和退货 | ERP-lite 发起并完成库存事务 | ERP 发起；MES-lite/WMS 执行并回传实绩 |
| 附件和生产文档 | MES-lite 主控 | MES-lite 主控，外部系统通过资源标识引用或同步 |

## 7. 前端工作区与模块开关

### 7.1 推荐工作区

| 工作区选项卡 | 包含能力 | 默认状态 |
| --- | --- | --- |
| **MES** | 物料、BOM、工艺、设备、生产订单、派工、报工、质量、追溯、生产库存、文档 | 默认启用 |
| **MRP** | 需求、BOM 展开、缺料、齐套、建议生产、建议采购、负荷提示 | 默认启用，可关闭 |
| **ERP** | 客户、供应商、销售订单、来料商业字段、发货、退货和参考计价 | 默认启用，可关闭 |

系统设置、权限、AI、附件、审计和数据维护属于应用壳固定共享功能，不参与 MES、MRP、ERP 业务页面归属。工作区切换只改变业务导航，不改变同一业务对象的 ID、附件、权限和审计链。

管理员可在“系统设置 → 导航与工作区”为每个业务页面选择唯一所属工作区，并分别配置各工作区的一级菜单顺序、页面显示名称和组内页面顺序。一级菜单顺序统一应用于桌面侧栏、顶部导航和移动端菜单。同一页面不能同时出现在 MES、MRP、ERP 菜单中；其他工作区需要该数据时，通过详情关联、快捷入口或业务流程跳转访问，不复制一级菜单。内部页面 ID、路由、权限资源和数据语义保持不变，历史重复配置会按系统默认主归属自动收敛。

当前 MRP-lite 仅将“BOM 全览”作为已有的结构与用量检查入口。需求展开、净需求、缺料、齐套、计划订单和采购/生产建议形成独立页面后，再逐项加入 MRP 菜单；不使用重复挂载 MES、ERP 页面来制造功能完整的假象。

### 7.2 模块行为

- 关闭 **MRP-lite**：隐藏缺料、齐套、建议单和计划参数，不影响 BOM、生产订单和库存。
- 关闭 **ERP-lite**：隐藏客户/供应商维护、销售订单及价格金额字段；MES 仍可通过接口接收外部需求和来料任务。
- 关闭商业字段不等于删除数据，重新启用或接口同步时历史关系仍可追溯。
- 页面、API、权限和搜索都必须读取同一模块配置，不能只隐藏前端按钮。

## 8. 明确不建设的范围

以下能力不进入 MES-lite 主产品路线：

- 总账、会计凭证、财务报表、应收应付、资金和付款。
- 发票、税务、报税、薪资和完整人事管理。
- 完整 CRM、营销自动化、售后服务和供应链协同平台。
- 多组织合并报表、集团财务和跨法人结算。
- 高级需求预测、S&OP、复杂 APS 和全局最优排程。
- 专业 WMS 的波次、自动仓设备调度和运输路线优化。

这些能力通过稳定业务标识、状态回传和集成接口与专业系统协作，不在 MES-lite 内创建简化但不可维护的平行实现。

## 9. 建设优先级

1. **补强 MES 核心闭环**：工序报工、投退料、质量、不合格处置、批次/序列追溯、设备事件。
2. **统一共享主数据**：物料变体、版本化 BOM、工艺路线和单位规则，消除不同页面的隐含口径。
3. **建设 MRP-lite 垂直切片**：需求输入 → BOM 展开 → 可用量 → 缺料 → 建议生产/采购，全流程可解释、可追溯。
4. **收敛 ERP-lite**：保留独立运行必要能力，增加模块开关和外部 ERP 接口，不继续向财务系统扩张。
5. **统一边界页面**：按字段所有权拆分展示区和权限，而不是复制来料、库存、物料和工作台页面。

## 10. 新功能立项检查

每个新功能在开发前必须回答：

1. 业务事实由 MES、MRP、ERP 还是共享平台负责？
2. 独立运行和对接 ERP 两种模式下，谁是数据主控方？
3. 关闭 MRP-lite 或 ERP-lite 后，页面、API、权限和字段应如何表现？
4. 是否能复用现有物料、库存、附件、搜索、权限和页面框架？
5. 是否进入了本文件明确“不建设”的范围？如果是，应改为接口而非本地模块。

验收时至少确认：不存在重复主数据或重复库存账；跨工作区仍使用同一记录 ID；所有同步和状态变化可审计；MES 在 ERP-lite 关闭时仍能完整执行生产。
