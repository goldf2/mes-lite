# 下一步任务

## 已完成：发布 v0.1.466 显式成本运行选择

- 目标：发布生产订单显式成本运行选择，核对服务端归属校验、订单成本快照和旧回退行为。
- 验收：候选 CI `33999128022`、main CI `33999726162` 通过；Con01 `bnqbb12qoyhvvtkm8xbcaqjx` finished，容器版本、备份、健康检查和三条持久卷已只读核对。
- 回滚边界：本版本无数据库迁移；部署期间旧容器保持 healthy，切换完成后仅新容器运行；如需回滚，保留数据库和持久卷，不删除订单成本快照。

## 已完成：v0.1.463 生产滚动更新

- 精确 SHA `ea126da453e6fa8640734da1a9248d2c61bfacdd`；候选 CI `33993847398` 和 main CI `33994513463` 成功。
- Coolify 部署 `omufsu03hc2k9l7iy9crpi14` finished；最终容器 healthy，旧容器已退出，`SOURCE_COMMIT` 与镜像均匹配。
- 最终迁移前备份归档已生成并通过 SHA-256 校验；`/api/health` 和 `/api/health/ready` 返回 200，迁移 96，持久卷映射未变。

## 已完成：生产工艺与成本快照 v0.1.462

- 目标：让 BOM、工艺路线、工序、工作中心和各工序成本在生产订单、生产实绩、快捷日报和物料全景中形成可追溯、可冻结链路。
- 验收：本地门禁通过；`ci/0.1.462` 精确提交 `5508d3d267c3d57a3803bdd705c191b182e3dffb` 的候选 CI `33988801209` 与 main CI `33989565361` 全部成功；同一 SHA 已推入 `main` 并自动部署，Con01 迁移/健康接口已验证。备份开关加载的重试由 v0.1.463 承接。
- 回滚边界：新增列和索引均可空；若生产已产生新快照，先停止新写入并备份，不直接删除迁移或倒写历史事实。

## 已完成：生产订单显式选择成本运行

- BOM 成本计算页已支持按物料明确选择已发布 BOM 版本和路线；生产订单创建页新增成本运行选择器，按时间倒序列出同一 BOM 的已保存运行。
- 服务端校验成本运行属于当前物料/BOM且工艺路线仍存在，并把选择冻结到订单；不选择时仍按默认路线取最近匹配运行。
- 本地生产订单、BOM 生命周期、实绩上下文、BOM-工艺成本联动和模块边界验证已通过；候选、主线和 Con01 已完成。

## P2. 多路线生产执行与实际工序成本

### v0.1.467 当前发布门禁

- 本地实现已完成：生产日报可选择已保存成本运行并冻结路线/成本快照；生产订单和日报校验成本运行的物料归属；BOM 成本对象改为精确匹配；物料全景安全兼容唯一可归属的历史运行。
- 本地定向验证和 SOP/开发文档门禁已通过；候选 CI、main 和 Con01 仍待本轮发布后补充真实证据。
- 发布前不得把本版本写成已部署；只操作 Con01，不访问 AL02。

- 在现有成本运行选择基础上，允许生产订单按已发布 BOM 选择具体工艺路线，并对路线、工序、工作中心和费率形成一致的执行上下文。
- 复核生产实绩/快捷日报是否需要按工序登记实际工时、机时、能源和例外成本，并继续冻结实绩快照。
- 验收：同一物料多路线候选、成本运行归属校验、订单/实绩/日报快照和成本汇总均有事务回归；不改写历史已确认事实。

## P2. 发布统一物料编码 v0.1.460

- 目标：完成历史单位保留回归与文档，推送 ci/0.1.460；v0.1.459 候选已取消，不推 main。
- 验收：精确提交的 MES-lite CI 成功后才推进 main；Con01 公开版本与 readiness 正确。
- 不重复 v0.1.458 发布；其精确 SHA 与两轮 CI 已通过，见 CURRENT_STATE。

## P1. 生产兼容编码数据统一

- 当前阻碍：Mac 锁屏，无法操作已登录的 MES/Coolify 页面；等待解锁，不能声称生产数据已修复。
- 步骤：确认新版本 → 新鲜备份 → 数据工具读取物料和 Product 预览 → 无冲突时执行 → 复查 pending=0、原 Product ID/业务数量保持。
- 歧义映射、多个 Product 指向一个物料、占用编码均保持原记录，必须逐项确认；不按前缀或排序猜测合并。
- 只操作 Con01；不访问 AL02，不重写已发布 BOM 或历史快照。

## P2. 既有运维警告

- readiness 备份超过 26 小时，生产数据变更前先补受控备份。
- Collabora 未配置保留降级，不属于本次统一编码范围。
