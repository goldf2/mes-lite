# 下一步任务

## P1. 完成 v0.1.458 候选门禁

- 目标：提交已经本地验证的 1280px 明细可读性、隐藏模块状态清理和 `SplitWorkspace` 站点存储降级修正，推送 `ci/0.1.458`。
- 验收标准：候选 `MES-lite CI` 对应精确提交 SHA 且全部通过；失败时不进入 `main`。

## P1. 完成 v0.1.458 候选、主线与 Con01 验收

- 目标：将已通过本地门禁的精确提交推送 `ci/0.1.458`；候选 CI 通过后仅将同一 SHA 推进 `main`，等待 Con01 Coolify 部署。
- 验收标准：记录候选与主线 `MES-lite CI` run ID、精确 SHA、结论和时间；公开静态版本为 `v0.1.458`，`/api/health/ready` 返回 200；在 1280px 核对右侧明细可读宽度、隐藏模块状态清理和默认全收起，在禁用站点存储场景核对分栏可用，并确认真实物料的 BOM 与锯切成本仍可见；窄屏上下排列且无横向溢出。
- 安全边界：只访问 Con01，不访问 AL02；页面验收只读，不修改生产业务数据。

## P2. 处理生产运维警告

- 目标：补做受控备份并复核 readiness 的 `backupFreshness`；Collabora 是否配置另行决策。
- 验收标准：备份时效恢复 `pass`，或在运维记录中明确接受的 RPO 与下一次备份时间。
