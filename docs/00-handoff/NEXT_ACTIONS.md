# 下一步任务

## P1. 完成 v0.1.457 本地与候选门禁

- 目标：完成研发文档、SOP 影响、版本说明、差异检查和本次直接相关回归，提交并推送 `ci/0.1.457`，确认 GitHub Actions 运行的 SHA 与候选提交完全一致且全部通过。
- 验收标准：记录候选提交 SHA、`MES-lite CI` run ID、结论和时间；失败时不进入 `main`。

## P1. 推进 main 并验证 Con01

- 目标：候选 CI 通过后将相同提交推进 `main`，等待 Con01 Coolify 部署。
- 验收标准：公开静态版本为 `v0.1.457`，`/api/health/ready` 返回 200；在 1280px 以上核对左纵览、右明细、拖动分隔、默认全收起、连续切换单组展开，并确认锯切成本和 BOM 仍可见；在窄屏确认上下排列且无横向溢出。
- 安全边界：只访问 Con01，不访问 AL02；页面验收只读，不修改生产业务数据。

## P2. 处理生产运维警告

- 目标：补做受控备份并复核 readiness 的 `backupFreshness`；Collabora 是否配置另行决策。
- 验收标准：备份时效恢复 `pass`，或在运维记录中明确接受的 RPO 与下一次备份时间。
