# 下一步任务

## P1. 完成 v0.1.455 候选发布门禁

- 目标：完成 Lint、SOP、研发文档、发布说明和差异检查，提交并推送 `ci/0.1.455`，确认 GitHub Actions 运行的 SHA 与候选提交完全一致且全部通过。
- 验收标准：记录候选提交 SHA、CI run ID、结论和时间；失败时不进入 `main`。

## P1. 推进 main 并验证 Con01

- 目标：候选 CI 通过后将同一提交推进 `main`，等待 Con01 Coolify 自动部署。
- 验收标准：公开健康/版本为 `v0.1.455`；数据库包含两张欠库表；使用测试物料完成“零/不足库存发货 → 详情显示待补 → 同库位入库 → 批次成本补齐”，并确认生产领料仍拒绝超量。
- 安全边界：只访问 Con01，不访问 AL02；真实业务数据验证需使用可明确识别并可冲销的测试单据。

## P2. 用户确认后归档修复报告

- 目标：用户确认生产有效后，在 `/Volumes/project/修复报告/` 写入本问题修复报告。
- 验收标准：包含现象、根因、修改、验证和涉及文件。

## 暂缓任务

| 任务 | 暂缓原因 | 重新启动条件 |
| --- | --- | --- |
| 全局负库存开关 | 当前只授权发货先单后库 | 用户明确要求其他业务也允许负库存，并确认成本与批次规则 |
| 跨库位自动抵销欠库 | 会隐式改变位置事实 | 真实业务确认需要跨库位自动清算，并设计明确调拨审计 |
