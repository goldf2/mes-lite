# 下一步任务

## P1. 完成 v0.1.456 候选发布门禁

- 目标：完成 SOP、完整构建、研发文档、发布说明和差异检查，提交并推送 `ci/0.1.456`，确认 GitHub Actions 运行的 SHA 与候选提交完全一致且全部通过。
- 验收标准：记录候选提交 SHA、CI run ID、结论和时间；失败时不进入 `main`。

## P1. 推进 main 并验证 Con01

- 目标：候选 CI 通过后将同一提交推进 `main`，等待 Con01 Coolify 自动部署。
- 验收标准：公开健康/版本为 `v0.1.456`；打开有库存、BOM 与锯切成本的真实物料，全景首屏只显示关键指标和默认展开的档案/库存；其余模块可逐组及批量展开/收起，成本对象仍可见。
- 安全边界：只访问 Con01，不访问 AL02；本次为只读页面验收，不修改生产业务数据。

## P2. 用户确认后归档修复报告

- 目标：用户确认生产界面信息密度符合预期后，在 `/Volumes/project/修复报告/` 写入本次界面优化报告。
- 验收标准：包含原界面问题、修改、验证和涉及文件。
