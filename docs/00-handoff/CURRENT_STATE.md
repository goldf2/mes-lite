# 当前状态

最后更新：2026-09-04 22:16 CST

## 项目阶段

生产维护中。远端 `main` 与 Con01 生产当前已核验为 `v0.1.456`，提交 `51d0cd45638d53c4fe3abf5e3797816d819d5ae0`；候选 CI `33869015178` 和主线 CI `33871113932` 均通过。公开 `/api/health/ready` 于 22:17 CST 返回 `ready`。正在隔离工作树 `/private/tmp/mes-lite-panorama.IJGeuB` 开发 `v0.1.457` 物料全景左右主从布局，尚未提交或部署。

## 本次已完成

1. 物料全景宽屏改为左“纵览”、右“明细”的主从布局；左侧固定五项关键指标。
2. 右侧七组明细默认全部收起，一次只允许展开一组；切换明细自动收起上一组，再次点击当前组可关闭。
3. 左右布局复用公共 `SplitWorkspace`，支持指针拖动、方向键调宽和浏览器记忆；小于 1280px 自动上下排列。
4. 原有档案、库存、库位、文档、BOM、工艺、锯切成本、工单和追溯模块继续复用，不改变查询、接口或计算。
5. 布局设置继续控制右侧模块显示、顺序和密度，文案已同步主从结构。

## 当前验证结果

```text
npm run verify:material-bom-modules          通过
npx prisma generate + npx tsc --noEmit       通过
npx next lint（本次物料全景文件）             通过，0 警告
本地临时完整 SQLite + 真实浏览器窄屏           通过：默认全收起；BOM → 成本切换只保留一组
```

## 已知问题与风险

| 优先级 | 问题 | 影响 | 处理 |
| --- | --- | --- | --- |
| P1 | `v0.1.457` 完整领域门禁、候选 CI 和主线 CI 尚未运行 | 不能推进生产 | 完成本地直接相关验证，推送精确 SHA 到 `ci/0.1.457` |
| P1 | 1280px 以上拖动分栏和生产真实数据尚未验收 | 不能确认最终宽屏密度 | Con01 部署后用带 BOM 和锯切成本的物料进行宽屏验收 |
| P2 | 公开 readiness 的备份时效超过 26 小时 | 不阻塞应用，但恢复点偏旧 | 由运维补做受控备份并复核时效 |
| P2 | Collabora 未配置 | 在线表格原生预览不可用 | 与本次物料全景布局无关，继续保留兼容预览/下载降级 |

## 接手建议

1. 阅读根目录 `AGENTS.md`、本目录 `RUNBOOK.md` 和 `NEXT_ACTIONS.md`。
2. 从 `v0.1.457` 文档门禁、候选 CI、主线推进和 Con01 生产验收继续。
3. 规范项目工作树含用户未提交内容且落后远端，不得覆盖；本次隔离工作树为 `/private/tmp/mes-lite-panorama.IJGeuB`。
