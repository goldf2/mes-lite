# 当前状态

最后更新：2026-09-06  CST

## 项目阶段

当前持续目标进入“生产执行冻结工艺与成本依据、显式选择成本运行、并收紧生产滚动更新门禁”阶段。v0.1.462 已提交为 `5508d3d267c3d57a3803bdd705c191b182e3dffb`，候选 CI 与 main CI 均通过，`main` 已自动部署到 Con01 并成功应用 96 条迁移。v0.1.463 已提交为 `72beb984f58c46f98589568ff167eca94d290b2a`，v0.1.464 已提交为 `ea126da453e6fa8640734da1a9248d2c61bfacdd`，v0.1.465 文档已收口最终运行证据。当前工作树实现 v0.1.466 的显式成本运行选择，上一阶段的统一物料编码候选仍以 v0.1.460 文档保留，不能把它写成已完成的生产数据修复。

## 当前改动与验证

- `BomCostRun` 新增工艺路线完整快照；成本运行明细按工序、工作中心和成本分类保存。
- 新建 `ProductionOrder` 时按目标物料、明确 BOM 和默认路线冻结 BOM、路线、工序、工作中心与匹配成本运行；订单详情优先读取快照。
- `ProductionOrderActual` 和 `DailyProductionReport` 复制订单/成本快照，生产执行不再依赖后续可变路线解释历史。
- 物料全景按 `bomId` 映射最新成本运行，展示对应路线和工序明细；锯切成本对象覆盖同名工序时不重复计费。
- 纯成本规则与路线快照序列化器已下沉到 `lib`，模块边界与架构基线不再存在本轮新增的深层跨模块导入。
- 工作中心新增人工、机时、能源默认费率；工序成本按“工序正值 > 工作中心默认 > 页面费率”回退，并在新成本运行/生产执行快照中保存实际采用费率。
- 已通过 Prisma Client 生成、TypeScript、生产构建、BOM 成本/路线联动、订单快照、实绩继承、快捷日报快照、物料全景、模块边界、架构基线和 50 项 CI 领域基线；另补回归证明冻结路线即使没有工作中心也不会回退当前路线，历史成本对象名称相似但不相等时不会误跳过工序。
- Con01 当前运行 `c099394574c4cb59d8a60b62bc93701e7686f286` 的最终容器 `jhjbwdumt46lsy4x2zj03llo-223851761552`，healthy；`/api/health` 与 `/api/health/ready` 返回 200，迁移为 96，三条持久卷保持 `/opt/mes-lite/{data,uploads,backups}` 映射，旧容器已退出。
- Coolify 部署队列 `eotbyz193mldkvnkb1a89jjv` 已完成并加载 `MES_LITE_PRE_MIGRATION_BACKUP_ENABLED=true`；启动窗口为 120 秒，最终备份 `mes-lite-backup-2026-09-05T23-10-50-652Z-934d9d76.tar.gz` 已生成并通过 SHA-256 校验（`8b57047ddb42a60d78db370a264db4bae647c150f0a7f9e2c1e1f88564f57343`），包含 219 条附件记录和 664 个文件。
- v0.1.463 将 Docker readiness 健康检查启动窗口从 15 秒延长至 120 秒，并以运维基线锁定，避免备份快照和 Prisma 启动耗时触发误回滚。

## 待验证与边界

- v0.1.466 候选、main CI 和 Con01 部署仍需按同一发布门禁验证；当前显式成本运行改动尚未写入生产。
- 新增快照字段均可空，旧记录读取时兼容当前默认路线；这只是历史兼容，不代表旧记录已经补齐冻结成本。
- 工作中心费率迁移以 0 初始化历史工作中心；0 表示未配置，当前不能表达“显式覆盖为零”的特殊费率。
- 能源费率统一按台·小时计；生产订单/日报仍在多条有效工艺路线时自动取默认路线，尚未提供执行时显式路线选择器。
- 旧成本运行若没有 `bomId`，物料全景不会把它猜测归入多个 BOM；需要单 BOM 消歧后再补兼容映射。
- 成本运行在未显式指定时仍取默认已发布 BOM/路线；v0.1.466 已在本地提供显式选择，但生产执行尚待发布验收。
- v0.1.465 的 Con01 滚动更新、备份开关和健康窗口已经完成并核对；后续版本仍须先确认新鲜备份、健康窗口和真实页面，未访问 AL02。
- 保留 README、旧 Excalidraw 删除、`.gitfinder`、AI 图标、库存数字孪生文档和 `tmp` 等无关本地修改，不得混入本次提交。

## 唯一下一步

完成 v0.1.466 候选 CI、main 推进和 Con01 只读验收；随后继续多路线生产执行与实际工序成本完善。
