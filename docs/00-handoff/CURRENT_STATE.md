# 当前状态

最后更新：2026-09-04 23:04 CST

## 项目阶段

生产维护中。Con01 生产最近核验版本仍为 `v0.1.456`，提交 `51d0cd45638d53c4fe3abf5e3797816d819d5ae0`；公开 `/api/health/ready` 于 22:57 CST 返回 `ready`。`v0.1.457` 提交 `6e1491915c034ec05bfbcca996bc37304c2d5082` 的候选 CI `33883741692` 已通过并以同一提交推入远端 `main`；独立审查发现三项布局边界后，主线 CI `33886361407` 已主动取消，由隔离工作树 `/private/tmp/mes-lite-panorama.IJGeuB` 中完成本地验证的 `v0.1.458` 取代，尚未提交、推送或部署。

## 当前改动范围

1. `v0.1.457` 已建立左“纵览”、右“明细”的主从布局，右侧七组明细默认全部收起且一次只展开一组。
2. `v0.1.458` 将左栏最大占比由 48% 收紧为 42%，并让“档案与库存”到超宽屏才内部并排，避免 1280px 宽屏下右侧复杂明细过窄。
3. 隐藏当前展开的明细模块时同步清空活动状态，避免模块重新显示后遗留不可见展开状态。
4. 公共 `SplitWorkspace` 对浏览器站点存储读写异常增加降级：读取失败使用默认比例，写入失败仍保留当前会话布局。
5. 原有档案、库存、库位、文档、BOM、工艺、锯切成本、工单、追溯查询、接口和计算均不改变。

## 当前验证结果

```text
v0.1.457 候选 MES-lite CI 33883741692         通过，SHA 6e1491915c034ec05bfbcca996bc37304c2d5082
v0.1.457 主线 MES-lite CI 33886361407         已取消：由 v0.1.458 边界修正取代
v0.1.458 定向回归、TypeScript、研发文档、SOP    通过
v0.1.458 定向 Lint                              通过：0 error；1 条既有 no-img-element 警告
v0.1.458 1280px 最大分栏真实浏览器              通过：42/58，右侧约 690px，文本正常，无横向溢出
v0.1.458 隐藏/恢复活动模块真实交互               通过：活动状态清空，恢复后保持收起
v0.1.458 390px 真实浏览器                       通过：上下排列，scrollWidth = 390
v0.1.458 候选 CI、提交、部署、生产验收          尚未进行
```

## 已知问题与风险

| 优先级 | 问题 | 影响 | 处理 |
| --- | --- | --- | --- |
| P1 | `v0.1.458` 尚未形成精确提交并通过候选 CI | 不能作为最终生产版本 | 提交后推 `ci/0.1.458` 并核对 CI SHA |
| P1 | Con01 生产仍待发布和真实数据验收 | 不能把本地验证写成生产完成 | 候选与主线 CI 通过后部署，核对真实 BOM 与锯切成本明细 |
| P2 | 公开 readiness 的备份时效超过 26 小时 | 不阻塞应用，但恢复点偏旧 | 由运维补做受控备份并复核时效 |
| P2 | Collabora 未配置 | 在线表格原生预览不可用 | 与本次物料全景布局无关，继续保留兼容预览/下载降级 |

## 接手建议

1. 阅读根目录 `AGENTS.md`、本目录 `RUNBOOK.md` 和 `NEXT_ACTIONS.md`。
2. 先完成 `v0.1.458` 三项边界修正的本地门禁和候选 CI，再以同一 SHA 推进主线与 Con01 验收。
3. 规范项目工作树含用户未提交内容且落后远端，不得覆盖；本次隔离工作树为 `/private/tmp/mes-lite-panorama.IJGeuB`。
