# 当前状态

最后更新：2026-09-05 12:00 CST

## 项目阶段

Con01 生产公开 readiness 正常，当前主线为 v0.1.458，提交 `08b7cf5ec5b786daafadc64c1b9a4c642500998a`。该提交候选 CI `33887668557`、主线 CI `33890614050` 均已通过，纠正此前“尚未推送”记录。v0.1.459 正在隔离工作树 `/private/tmp/mes-lite-unified-code.QpageH` 中完成统一物料编码修复，尚未推送或部署。

## 当前改动与验证

- Material.code 是唯一业务编码；兼容 Product 保持 ID，新增/安全复用时显式关联 materialId 且 sku 与主档相同。
- BOM、成本、全景、日报查询优先显式物料 ID；锯切选择器只提供物料主档，避免双轨选项。
- 改名/导入同步既有兼容记录；目标编码占用时事务回滚。真实 MAT- 编码不被截断，扫码不再把 MAT-A 当作 A。
- 现有数据工具先展示物料及 Product 变化，存在歧义、重复目标或编码占用时禁止执行；只更新编码及绑定，不改库存数量、BOM 版本/数量和历史快照。
- 定向回归（模型收敛、编码规范化、BOM/成本、快捷日报、工艺、扫码、销售）、TypeScript、模块/架构边界已通过。生产构建由候选 CI 执行，未在本机重复构建。

## 待验证与边界

- 当前 Mac 锁屏，已提示用户解锁；无法检查已登录的生产/Coolify 页面。尚未读取本次生产映射预览、执行数据修复或完成真实 UI 验收。
- 2026-09-05 11:57 CST 公开 readiness 为 ready，93 个迁移已应用；备份超过 26 小时、Collabora 未配置仍有警告。
- 生产数据修复须在新版本部署后先备份、查看当前预览，再执行无冲突的受控整体转换；冲突交人工确认，不自动删除/合并记录。
- 规范项目工作树保留用户未提交内容；只访问 Con01，禁止 AL02。

## 唯一下一步

完成 v0.1.459 本地门禁和文档，提交到候选分支并核对精确 SHA 的 CI。通过后同 SHA 推 main，核验 Con01 部署，再完成有备份的数据预览与统一编码。
