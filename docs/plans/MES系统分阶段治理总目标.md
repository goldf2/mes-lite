# MES-lite 系统分阶段治理总目标

状态：执行中

建立日期：2026-08-12

当前阶段：阶段 4 持续治理（`v0.1.396` 已形成仓库开发冻结候选，并将历史切片中的重复外部待办收敛为 G0–G6 七个生产验收门禁；既有 CI、轻量 Git/Docker 发布树、统一 MES 工作台、业务闭环、细粒度权限、181 流程 SOP、7 个跨页面视频 SOP、备份恢复、Schema/模型预检和商业交付基线保持有效）；最新生产一致备份、生产 Schema/映射签字、真实岗位审批、Coolify 部署与回切、OSS 文档/视频发布、异地灾备及历史安全清理仍须按[开发冻结与生产验收交接清单](../delivery/开发冻结与生产验收交接清单.md)完成，不能把本地代码完成表述为全局验收完成

### 最终完成门禁

历史阶段中的未勾选项继续作为原始审计轨迹，不再逐条重复计算进度。全局目标只在以下七项全部有证据和签字时完成：G0 仓库冻结、G1 最新生产一致备份、G2 Schema 与 Product→Material 收敛、G3 真实岗位与权限、G4 Coolify 部署与回切、G5 OSS 文档与视频发布、G6 异地灾备与历史安全清理。当前仅 G0 具备仓库级自动证据，G1–G6 均不得由本机模拟替代。

## 1. 全局目标

在不推倒现有 Next.js 单体、不破坏单厂 SQLite 运行基线的前提下，将 MES-lite 从“功能较多的制造管理后台”治理为“具有质量、批次、库存状态和可信审计闭环的轻量 MES”，并形成可验证、可备份、可恢复、可升级的单厂商业交付基线。

所有阶段必须同时满足：

1. 先写回归或结构守卫，再修改实现。
2. 页面、API、权限、模型和文档同步更新。
3. 数量、成本、库存状态和批次变化可通过业务流水与审计回放。
4. 每个交付版本通过类型检查、Lint、生产构建和受影响领域的临时数据库回归。
5. 每个阶段保留明确回滚点；生产数据迁移前必须完成备份与恢复演练。

## 2. 阶段路线

| 阶段 | 目标 | 主要交付物 | 完成标准 |
| --- | --- | --- | --- |
| 0 | 安全、数据与运维止血 | 附件对象级鉴权、可信操作人、认证防护、仓库数据清理、一致性备份和恢复演练 | 已知高风险访问路径关闭；测试和生产数据不进入 Git；备份可恢复到新实例 |
| 1 | 产品与核心数据定盘 | 单厂 MES 边界、`Material` 唯一主档、旧生产模型盘点和退出方案、统一状态机 | 新代码不再增加 `Product/Material` 和新旧生产兼容关系；迁移可校验、可回滚 |
| 2 | MES 现场事实闭环 | 内部批次、批次谱系、待检/可用/冻结库存、检验任务、不合格处置 | 可从供应商批次正向追到成品和客户，也可从客户退货反查原料和检验结论 |
| 3 | 角色任务型工作区 | 操作员、仓管、质检、计划员和管理员工作台；确认/放行/冲销等动作权限与数据范围 | 移动端优先呈现岗位待办；管理员功能退出普通车间导航；越权用例全部返回 403 |
| 4 | 商业交付基线 | 当前事实文档、CI、监控、备份保留策略、升级/回滚手册和交付验收表 | 新环境可自动构建；发布可回滚；恢复演练有 RPO/RTO 记录；交付范围不混入规划功能 |

## 3. 当前执行状态

### 阶段 0A：附件与审计身份（v0.1.345）

- [x] 附件读取、上传、预览、缩略图、更新和归档同时校验附件权限与所属业务资源权限。
- [x] 校验 `ownerType/ownerId` 是否对应受支持且真实存在的业务对象。
- [x] 暂存附件只允许创建它的登录账号读取、绑定和清理。
- [x] 禁止通过 `/uploads/*` 绕过附件 API 直接读取持久文件。
- [x] AI 单据识别复用同一附件对象级权限。
- [x] 生产、转移、来料、发货、退货、成本和兼容过账的操作人只来自服务端登录会话。

### 阶段 0B：认证与管理员初始化（v0.1.346）

- [x] 公开注册默认关闭；密码和微信注册永不自动获得管理员身份。
- [x] 初始管理员通过一次性服务器令牌显式安装，已有管理员后拒绝重复安装。
- [x] 账号连续失败持久锁定，登录、注册和安装入口按来源持久限流并返回重试时间。
- [x] 生产 Cookie 显式启用 `Secure`，所有写 API 在 Middleware 执行 Origin 同源校验。
- [x] 登录页依据服务端注册开关显示或隐藏注册入口。

### 阶段 0C：运行时数据与灾备（v0.1.347-v0.1.348）

- [x] 停止跟踪 SQLite 数据库、journal/WAL/SHM 与 TypeScript 构建缓存，保留当前本地数据。
- [x] 建立 SQLite 一致快照、附件引用校验、逐文件 SHA-256、保留策略和非覆盖恢复候选。
- [x] 增加 liveness/readiness 分层，readiness 覆盖数据库、失败迁移和持久目录可写性。
- [x] 对齐 Compose、Dockerfile、Coolify 挂载、迁移前备份、每日调度和恢复演练手册。
- [x] 依据当前 Coolify 应用配置，将生产来源和微信回调示例统一为 `mes.szxiangshu.com`。

### 阶段 0D：写操作审计覆盖（v0.1.378）

- [x] 以 TypeScript AST 枚举 134 个 `POST / PUT / PATCH / DELETE` 处理器，强制每条写接口声明审计、服务委托或具体豁免原因。
- [x] 补齐附件、身份、订单、派工、成本、旧工单收尾、扫码盘点和人员审批的审计缺口。
- [x] 对 14 个高风险命令强制传递当前请求审计上下文，并将业务变化与审计写入同一 SQLite 事务。
- [x] 临时数据库回归验证正常、失败与幂等分支；覆盖守卫进入 CI，关闭审查项 P2-04。

### 阶段 0 维护窗口待办

- [x] `v0.1.352` 增加不可覆盖的恢复演练报告命令，并在本机隔离数据上验证 SQLite、4 条附件引用、21 个归档文件及候选 RPO/RTO；未安装 Docker，因此不把候选通过表述为新实例恢复通过。
- [ ] 历史 commit 仍含旧 SQLite 快照；需在维护窗口完成备份、协作者通知、Git 历史重写、远程强制更新及账号/密钥轮换。
- [x] `v0.1.362` 在 Coolify 持久挂载 `/app/backups`，启用迁移前备份和每日一致备份，首份归档/附件/SQLite 校验通过，并完成非覆盖恢复候选演练（RPO 197.472 秒，候选 RTO 11.460 秒）。
- [ ] Coolify 当前没有 S3 Storage；提供独立对象存储或另一主机凭据后，完成异地上传、取回、SHA-256 和恢复候选验收。
- [x] `v0.1.366` 在本机隔离生产构建复制生产恢复候选，完成全附件哈希、当前迁移、readiness、临时管理员登录、物料/库存/生产单和附件原文件抽查，完整应用 RTO 1.582 秒；真实 Coolify 挂载切换仍待维护窗口。

### 阶段 1A：产品合同与旧写入止增（v0.1.349）

- [x] 固定单厂轻量 MES 产品合同，MRP-lite/ERP-lite 只作为计划与经营辅助能力，不作为顶层工作区或独立系统，多租户 SaaS 暂停。
- [x] 固定 `Material` 唯一用户侧物品主档；不再创建或自动补齐 Product 独占库存。
- [x] 禁止新建旧 `DailyProductionReport`，只保留历史记录维护与冲销。
- [x] 物料工单拒绝旧领料、报工和入库 URL，生产事实统一写入 `ProductionOrderActual`。
- [x] 增加只读模型收敛审计和结构守卫，Product 依赖允许集合只能缩小。

### 阶段 1B：外键迁移与统一状态机

- [x] `v0.1.350` 将新 Material 工单写入状态统一为 `DRAFT → RELEASED → IN_PROGRESS → COMPLETED/CANCELLED`。
- [x] 历史订单状态在列表筛选、派工、仪表盘和 AI 查询中归并到新口径，不批量改写生产数据。
- [x] `v0.1.353` 增加 Product 及 5 张旧依赖表的可空 Material 投影，新写入尽可能携带投影，不在部署时自动猜测生产数据。
- [x] `v0.1.353` 交付生产镜像内 `audit / plan / apply` 维护命令、人工确认契约、单实例门禁、迁移前备份、成功/失败报告、对账与指定备份恢复回归。
- [x] `v0.1.368` 增加维护窗口前 `preflight` 只读门禁，成功/失败报告记录映射和数据库前后 SHA-256；对 2026-08-13 生产恢复候选完成匿名审计，确认 13 个唯一候选、3 个无候选、2 张缺主产出 BOM 和 0 个高风险 Product 库存，但该旧候选不替代最新生产刷新或业务签字。
- [x] 完成生产备份挂载、可验证一致备份和非覆盖恢复候选演练；异地副本仍待独立存储凭据。
- [x] `v0.1.380` 在 Product 退出前固定过渡期库存归属：迁移前拒绝无归属/双归属历史行，数据库新建/更新触发器保证每条 `Stock` 恰有一个 Material 或旧 Product 所有者；Product-only 仍可由签字后的维护命令原子切换为 Material。
- [x] 在 v0.1.376 零漂移生产恢复候选的隔离副本应用第 83、84 个迁移：62 条库存、18,600 汇总数量、0 成本和关键业务表统计均不变，归属保持 Material-only 46、Product-only 16、非法 0，`PRAGMA quick_check=ok`；该旧副本仅证明迁移兼容性。
- [ ] 在生产运行只读审计，人工确认所有 `Product → Material` 映射和 Product 独占库存处置。
- [ ] 在人工确认后执行生产回填与核对，再增加物理 Material 外键、收紧非空并逐步退出 Product 外键。
- [ ] 记录每批迁移前后统计、回滚点和生产验证，不在本地数据上推断生产结论。

### 阶段 2A：生产产出批次、质检与库存状态（v0.1.354）

- [x] 确认生产实绩时为每项产出生成内部批次、批次余额、批次交易和待检任务。
- [x] 将总库存、库位库存和成本层拆分为 `AVAILABLE / QUARANTINE / HOLD` 状态，待检和冻结库存不参与领料、发货或普通调整。
- [x] 在生产订单详情提供整批合格放行和整批不合格冻结；判定由独立 `quality.update` 权限和服务端事务保护。
- [x] 质量判定同步更新检验、批次、库存、成本层和库存流水，并保留操作人、抽样数据和备注。
- [x] 用全新临时库验证 71 个迁移、生产待检/放行/冻结、FIFO 和状态隔离；用迁移副本验证旧库可升级，原库未改动。
- [x] 生成 7 张真实浏览器操作截图，并纳入 v0.1.354 作业指导书。
- [x] `v0.1.356` 发货与退货选择并保留成品批次，形成供应商到客户及退货回流的正反向追溯。
- [x] `v0.1.357` 增加部分放行、复检、让步接收、返工、报废、解冻及跨工单质量待办。

### 阶段 2B：来料、生产投入与批次谱系（v0.1.355）

- [x] 来料接收逐行生成唯一内部批次，保留供应商批号、来料行、库位、数量、核算量与成本来源。
- [x] 流程转移同步转移开放批次的库位余额；确认与冲销成对，不改变总库存或总成本。
- [x] 生产投入按来源库位可用批次 FIFO 分配，并将全部投入批次连接到每个产出批次。
- [x] 批次追溯弹窗可从产出反查上游来料/投入，也可从原料批次查下游产出；读操作复用 `stocks.read`。
- [x] 生产冲销恢复投入批次余额并撤销谱系；已被生产使用或余额变动的来料批次不得直接红冲。
- [x] 历史未追踪库存在首次分配时显式记为 `LEGACY_INVENTORY`，不伪造供应商批号或生产来源。
- [x] 用全新临时库验证 72 个迁移、两批来料 FIFO、批次配送、生产谱系、来源红冲阻断、生产/配送冲销和历史兼容；原库未改动。
- [x] 已完成真实浏览器截图、v0.1.355 全流程作业指导书、全量回归与生产构建；交付提交推送到远程后，本切片正式生效。

### 阶段 2C：客户发货、退货回流与退货质检（v0.1.356）

- [x] 确认发货按发货库位可用批次 FIFO 分配，并保存客户、发货单、发货数量、成本和累计退回量。
- [x] 新退货必须关联仍有剩余可退数量的原发货单；服务端锁定客户与物料并再次校验累计退货上限。
- [x] 退货收货建立独立 `RT-*` 隔离批次、来源分配、待检库存、检验任务、批次交易和库存流水。
- [x] 退货批次复用质量状态服务，支持整批合格放行与整批不合格冻结。
- [x] 批次追溯同时显示生产上下游、客户发货去向、退货来源和退货回流；历史发货只使用明确兼容批次。
- [x] 使用全新临时库验证 73 个迁移和完整履约闭环；原数据库未修改，真实 Coolify 未在本地执行。
- [x] 完成 10 张真实浏览器截图，并交付包含 88 个流程、90 页的 Markdown/DOCX/PDF 全流程作业指导书。
- [x] 已完成专项回归、类型检查、Lint、生产构建和文档双格式逐页渲染检查；交付提交推送到远程后，本切片正式生效。

### 阶段 2D：质量任务与不合格处置（v0.1.357）

- [x] 新增跨工单“质量任务”工作台，集中查询待检、待处置和全部质量记录，并支持按检验单、批次、物料及来源单据搜索。
- [x] 质量判定支持整批合格、整批不合格和部分判定；部分判定按数量拆分可用与冻结批次余额、库位余额和成本层。
- [x] 新增复检、转返工、返工完成送检、质量报废、让步放行和解冻放行；返工完成后必须再次进入待检，不能直接恢复可用。
- [x] 每次判定和处置写入独立 `QualityDisposition`，并同步批次交易、库存流水、库存状态和操作人；报废按批次比例减少数量、核算量及成本。
- [x] 库存状态扩展为 `AVAILABLE / QUARANTINE / HOLD / REWORK`，返工中库存不参与普通领料、发货或调整；生产冲销阻断多状态和已处置批次。
- [x] 权限拆分为质量任务查看、质量判定、复检/返工/报废处置、让步/解冻放行四个资源，页面按钮和 API 使用相同门禁。
- [x] 使用隔离临时库完成部分放行、复检、让步、返工、报废、解冻、成本与批次守恒验证；顺序应用前 73 个迁移后追加第 74 个迁移通过，原数据库未修改。
- [x] 新增 18 张真实浏览器截图；全流程作业指导书扩展到 106 个流程、108 页，DOCX 逐页重渲染检查，PDF 重渲染及关键页放大检查通过。
- [ ] 生产部署前按岗位审批结果配置四层质量权限，执行数据库/附件一致备份，并在真实 Coolify 维护窗口应用迁移和验收。

### 阶段 2E：跨业务批次搜索与正反向全景（v0.1.358）

- [x] 新增独立“批次追溯”页面，可按供应批号、内部批号、物料、供应商、客户及来料/生产/检验/发货/退货单搜索。
- [x] 从任意批次多跳展开生产投入/产出与客户退货回流，按相对层级显示上游、当前和下游，并汇总供应批次、客户和质量检验。
- [x] 全景只读取 `ACTIVE` 谱系，最多显示 100 条搜索结果与 300 个关联批次；切换搜索词会清空旧全景，防止误读。
- [x] 页面、两个只读 API 与既有相邻追溯统一复用 `stocks.read`，未增加重复权限资源。
- [x] 使用全新隔离临时库复现“供应批号 → 来料 → 生产 → 客户发货 → 退货待检”闭环，新增 6 张真实浏览器截图。
- [x] 全流程作业指导书扩展到 112 个流程、114 页；DOCX 与 PDF 均重渲染为 114 页并抽检封面、新增流程与治理边界。
- [x] 专项回归、页面注册、工作区导航、类型检查、Lint 和生产构建纳入本版发布门禁。
- [ ] 真实 Coolify 仅在维护窗口按一致备份、部署、只读搜索、反向退货追溯与回滚步骤验收；本机验证不冒充生产完成。

### 阶段 3A：岗位任务台与生产命令权限（v0.1.359）

- [x] 仪表盘首屏按当前账号的服务端有效权限生成生产、质量、仓储和系统任务区，没有对应动作权限的任务不显示。
- [x] 任务卡携带稳定 `task` 参数直达生产订单、质量任务、来料、发货和退货页面，并自动应用对应待办筛选。
- [x] 生产订单发布、生产实绩草稿登记、实绩确认过账和实绩冲销拆为四个独立权限资源；页面按钮和 API 使用同一门禁。
- [x] 增加基础访问、生产执行、生产管理、仓库作业、质量检验/处置/授权放行、生产计划和系统管理预置组；初始化只补缺，不覆盖已有企业配置。
- [x] 使用全新隔离数据库分别验证生产执行、生产主管、计划员、质检员和仓管员的任务可见性与 HTTP 403；原数据库未修改。
- [x] 完成桌面与 390px 移动端真实浏览器验收，新增 8 张截图并纳入 120 流程、122 页作业指导书。
- [x] `v0.1.361` 已增加本人/工作中心生产范围和库位库存范围，任务数量与页面/API 使用同一范围；部门/班组尚未独立建模。
- [ ] 真实 Coolify 只在岗位审批、数据库/附件一致备份与维护窗口就绪后部署；本地岗位演示不代表生产账号已配置。

### 阶段 3B：细粒度业务与系统资源（v0.1.360）

- [x] 权限资源由 30 个扩展为 48 个，`v0.1.369-v0.1.372` 增加设备事件、点检、维保与检验标准，`v0.1.374` 拆出来料收货/拒收与红冲，`v0.1.375` 再拆分七个物流状态命令，形成 61 个资源并继续按六区呈现。
- [x] 将流程转移从 `stats`、BOM 结构与版本从 `bomCost`，以及客商、员工、库位、单位、文档类别、工作中心、工艺路线、业务/显示/导航/AI 设置、归档、审计和数据工具从 `system` 拆为独立资源。
- [x] 页面注册、按钮状态、API、AI 工具、附件所属资源和业务单据定义使用同一资源边界；只读配置页禁用输入，不显示保存、测试或新增动作。
- [x] 新增仓库管理、工艺技术、设备维护、文档管理、销售发运、人员管理和权限管理员七个岗位预置组；权限管理员不自动获得业务或系统维护权限。
- [x] 已有角色设置、既有预置组、自定义组和个人覆盖在首次补齐时继承对应旧资源，不覆盖企业已经调整的设置；新安装直接使用细分预置。
- [x] 使用全新隔离 SQLite、真实 HTTP 和六类岗位账号验证业务资源互不串权及越权 403；无 Prisma Schema 变更，迁移仍为 74 个。
- [x] 新增 9 张真实浏览器截图；全流程作业指导书扩展到 129 个流程、131 页，DOCX 与 PDF 均完成逐页渲染检查。
- [x] 后续由 `v0.1.361` 增加本人/工作中心生产范围和库位库存范围；部门/班组尚未独立建模。
- [x] 后续由 `v0.1.361` 为个人例外增加原因、授权人、开始时间和失效时间，并建立到期自动失效与审计。
- [ ] 在真实 Coolify 维护窗口完成岗位审批、权限继承复核、一致备份、部署和越权验收；本地演示不代表生产账号已经配置。

### 阶段 3C：岗位数据范围与个人临时授权（v0.1.361）

- [x] 生产范围支持 `ALL / SELF / WORK_CENTERS`，库存范围支持 `ALL / LOCATIONS`；管理员固定全厂范围，旧账号无配置时兼容原全厂行为并在权限页显式标记。
- [x] 生产订单、派工、实绩、旧报表、统计和岗位任务按本人或工作中心过滤；来料、发货、退货、转移、库存、流水、批次追溯、质量任务和附件按库位范围过滤。
- [x] 写操作与状态动作在领域服务再次断言范围；跨范围请求返回 403，库存历史批次保留曾经授权库位的追溯可见性，质量动作只接受当前正余额所在的授权库位。
- [x] 新注册与首次微信登录账号默认使用本人生产范围及空库位集合，不再默认为全厂；本人范围要求绑定在职员工，工作中心和库位范围要求至少选择一项。
- [x] 个人覆盖改为有原因、授权人、开始时间和失效时间的临时授权，到期自动从有效权限中排除；历史永久覆盖保留兼容标记并要求管理员重新审批。
- [x] 权限、数据范围和个人例外更新均记录变更前后快照；新增隔离 SQLite、真实 HTTP、到期失效、跨范围附件和质量处置回归。
- [x] 使用隔离本地演示库完成 3 张真实浏览器截图；全流程作业指导书扩展到 132 个流程、134 页 Markdown/DOCX/PDF。
- [x] 已确认 Auto Deploy 曾在推送后把生产切换到 v0.1.361；数据库顺序应用到 76 个迁移且生产健康。当前已关闭 Auto Deploy，并固定到 `66a175d...`。
- [x] 已持久挂载生产备份目录、启用迁移前备份、建立每日定时任务、验证首份一致归档，并完成非覆盖候选恢复演练。
- [ ] 真实岗位账号的权限继承、数据范围和旧永久覆盖仍须企业审批；异地副本仍待对象存储凭据。

### 阶段 3D：归档对象二次授权（v0.1.367）

- [x] 归档列表同时要求归档读取权限和被归档对象的原业务读取权限，未授权类型不返回记录或完整字段。
- [x] 恢复与永久删除分别叠加原业务更新/归档权限，页面只显示服务端确认允许的动作。
- [x] 来料、生产订单、派工、发货和退货归档继续叠加人员生产/库存数据范围，跨范围动作返回 403。
- [x] 隔离 SQLite 与真实 HTTP 回归覆盖入口权限、原业务权限、最小字段、动作能力和库位范围。
- [ ] 真实岗位账号仍须在生产维护窗口按企业审批结果验收；本地专项回归不替代生产授权签署。

### 阶段 3E：来料收货与红冲命令分权（v0.1.374）

- [x] 草稿登记/编辑继续使用 `materialIn.create/update`，确认收货和拒收改用 `materialInReceive.update`，整单红冲改用 `materialInReverse.update`。
- [x] 来料页面按四类有效权限分别显示新建、编辑、收货/拒收和红冲按钮；仪表盘“待收货”任务只对有收货命令的账号显示。
- [x] 新安装仓库作业组可收货/拒收但不能红冲，仓库主管组另有有原因红冲；质量岗位不因读取来料而获得仓库命令。
- [x] 既有角色、自定义组、已修改内置组和个人例外不会由 `materialIn.update` 自动升级为高风险命令，维护窗口须显式审批授权。
- [x] 隔离 SQLite、真实 HTTP 和页面结构回归覆盖编辑账号 403、普通仓管收货成功/红冲 403、仓库主管红冲成功及原质量/库位闭环。
- [ ] 真实岗位账号仍须在生产维护窗口按企业审批结果授予并验收；本地预置和回归不替代仓储负责人签署。

### 阶段 3F：发货、退货与流程转移命令分权（v0.1.375）

- [x] 发货草稿、库存发出、客户签收和待发货取消分别使用 `shipment`、`shipmentDispatch`、`shipmentDeliver` 与 `shipmentCancel`。
- [x] 退货登记、接收入库和拒绝分别使用 `return`、`returnReceive` 与 `returnReject`；流程转移草稿、确认和冲销分别使用 `flowTransfers`、`flowTransferConfirm` 与 `flowTransferReverse`。
- [x] 普通仓管只获得库存正向执行，仓库主管另外获得取消/拒绝/冲销，销售跟单只获签收、取消和拒绝；仪表盘待办使用同一正向命令资源。
- [x] 既有角色、内置/自定义组和个人例外不从通用更新自动获得七个命令，维护窗口须依据岗位审批显式授予。
- [x] 发货、签收、取消、退货收货和拒绝写入请求级审计；取消、拒绝与移库冲销必须填写原因。
- [x] 隔离 SQLite、真实 HTTP、页面结构和领域回归覆盖通用编辑 403、普通执行成功、主管高风险动作成功、原因审计与库存/成本守恒。
- [ ] 真实岗位账号仍须在生产维护窗口按仓储、销售与生产负责人签字配置并验收；本地预置和回归不替代企业审批。

### 阶段 2F：设备基础运行事件（v0.1.369）

- [x] 设备基础资料严格拒绝运行状态字段，新设备固定从可用开始；状态只允许通过服务端事件状态机改变。
- [x] 建立开机、停机、故障、恢复和归档事件，原子保存来源/目标状态、原因、人员、时间与当前设备快照。
- [x] 恢复关闭最近未结束的故障/停机/维护事件并计算持续时间；恢复事件不重复展示同一时长。
- [x] 新增独立 `equipmentEvents` 权限资源，设备只读岗位可回放，命令岗位通过真实 HTTP 403/201 回归验收。
- [x] 设备台账、新建表单、故障命令、时间线和细粒度权限均使用真实浏览器截图；SOP 扩展到 144 个流程。
- [x] `v0.1.370` 交付周期点检计划、到期/逾期任务、完整项目记录、幂等、异常故障联动、现场附件、工作中心范围和独立第 50 个权限资源；真实浏览器覆盖 4 个点检流程。
- [x] `v0.1.371` 交付周期保养计划、计划/故障维修工单、开始/完成状态机、设备恢复、完整保养结果、备件库存/成本/FIFO 批次原子过账、工单附件和独立第 51 个权限资源；真实页面覆盖 6 个维保流程。
- [ ] 自动采集、停机原因、节拍和 OEE 仍是后续切片；必须先取得真实接口和口径签字。生产部署仍须维护窗口、备份与真实岗位验收。

### 阶段 2G：质量标准、抽样与趋势（v0.1.372）

- [x] 建立检验标准编码/版本、草稿/发布/停用生命周期和发布后不可变约束；同物料同来源只允许一个生效版本。
- [x] 支持生产与退货来源的全检、固定数量和比例抽样，并保存最低/最高抽样边界。
- [x] 新待检任务自动快照当时生效标准、建议抽样数和完整检验项目；复检沿用原任务快照。
- [x] 逐项结果与整单结论强一致，完成后不可覆盖；质量附件复用公共附件模块并叠加库位数据范围。
- [x] 趋势按日期、物料和来源聚合已完成任务，输出批次通过率、样本通过率、按日/物料汇总和失败项目排行。
- [x] 新增第 52 个权限资源、领域/真实 HTTP 回归、7 张真实页面截图及 161 流程多格式 SOP。
- [x] `v0.1.373` 已按物料的已发布 `MATERIAL_IN` 标准启用来料自动隔离、任务、判定、趋势和红冲联动；无标准物料保持原直接可用流程。
- [ ] SPC/过程能力、原因分类主数据和多级审批仍需真实质量体系口径后再建设；生产部署须在维护窗口完成备份和岗位验收。

### 阶段 2H：来料检验与仓库交接（v0.1.373）

- [x] 来料收货逐行检查已发布标准；受控物料进入 `QUARANTINE` 并自动建立质量任务，非受控物料继续进入 `AVAILABLE`。
- [x] 仓库岗位只能执行收货，不能借收货结果读取或判定质量任务；质检岗位继续受 `quality`、`qualityDecision` 和库位范围约束。
- [x] 待检来料红冲会取消未完成任务并反向恢复隔离库存；已合格且未被下游处理的批次可安全红冲，失败或已处理批次阻断直接红冲。
- [x] 来料来源纳入标准维护、质量任务、逐项判定与趋势筛选，不增加平行质量台账或重复权限资源。
- [x] 新增领域与真实 HTTP 回归、8 张真实页面截图及 169 流程 Markdown、DOCX、PDF、离线 Web SOP。

### 阶段 2I：生产实绩执行上下文快照（v0.1.376）

- [x] 实际设备候选限制为订单派工或默认路线工作中心内、未归档且可用/运行中的设备；服务端重新解析 ID，前端显示字段不作为事实来源。
- [x] 作业文件候选限制为已生效且适用于订单物料或工作中心的文档；保存标题、版本、状态、正文和附件清单快照，来源修改不回写历史。
- [x] 实绩必须分别满足“选择设备或填写原因”和“选择文件或填写原因”；确认服务和数据库触发器双重保护，已确认/已冲销上下文不可更新或删除。
- [x] 历史实绩通过加性迁移回填明确的迁移原因，不伪造过去未保存的设备或文件事实。
- [x] 桌面和手机复用一对多关系编辑骨架，3 张真实页面截图覆盖登记、快照核对和移动端；SOP 扩展为 181 个流程并同步四种格式。
- [ ] 自动采集、设备节拍、停机原因口径和 OEE 仍需真实接口与业务签字；本切片不宣称完成设备绩效管理。

### 阶段 3C：统一混合系统导航（v0.1.382）

- [x] 将应用壳中的 MES/MRP/ERP 工作区选项卡改为模块预留按钮：MES-lite 主按钮始终显示，MRP/ERP 可在系统设置中改名或隐藏，显示时不可切换菜单、权限或数据；系统版本移到左侧导航底部。
- [x] 保留全部业务页面、领域模块、页面 ID、权限资源和业务数据；MRP-lite/ERP-lite 继续作为计划与经营辅助能力责任边界。
- [x] 既有 `interface.workspaceNavigation.v1` 按 MES → MRP → ERP 合并到统一导航，保留首次出现的自定义名称和顺序，补齐新增页面，并禁用旧 MRP/ERP 容器。
- [x] “导航设置”管理模块按钮名称与 MRP/ERP 显隐，以及一级功能区顺序、页面名称和页面顺序；不再允许页面在三套系统之间分配。
- [x] 受影响 SOP 源更新为统一功能导航；开发期只生成当前 Markdown，DOCX/PDF/离线 Web 延后到阶段或商业交付冻结后一次性生成和验收。

### 阶段 2J：统一库存事务账本（v0.1.379）

- [x] 保留 `StockLog` 作为唯一库存事务事实源，不新增平行 `InventoryAdjustment` 或 `InventoryTransaction` 表。
- [x] 来料红冲、生产实绩冲销、历史生产冲销、流程转移冲销和旧工单取消退料统一复用库存冲销服务。
- [x] 原流水与冲销流水建立双向引用；服务和 SQLite 触发器共同阻止跨库存、数量/核算量/成本不守恒、重复冲销、解绑、篡改和删除。
- [x] 库存调整不再接受浏览器提供的操作人；服务器会话操作人、库存变化和审计日志在同一事务提交。
- [x] 库存流水页面显示“已由流水冲销 / 冲销原流水 / 关联来源流水”，并可按关联流水 ID 搜索。
- [x] 全新临时库与结构收敛生产恢复副本均通过第 83 个迁移；原始运行库副本仍被既有失败迁移 `20260812223000_add_material_projections_for_product_exit` 阻断，必须先按 v0.1.377 收敛流程处理，不能直接部署。

### 阶段 4A：CI、运维证据与商业交付基线（v0.1.362）

- [x] 新增 GitHub Actions：Node 20、`npm ci`、Prisma validate/generate、17 项领域/治理基线、类型检查、Lint 和生产构建。
- [x] `v0.1.362` 曾按维护窗口策略关闭 Auto Deploy 并固定精确提交；启用迁移前一致备份和每日北京时间 02:00 备份（当前容器 UTC 使用 `0 18 * * *`）。2026-08-15 的 `v0.1.381` 只读审计确认 main Webhook 自动部署现已开启，后续发布必须按这一现行事实执行 CI、备份和迁移门禁。
- [x] 邮件通知已启用部署失败、备份失败、定时任务失败、容器状态和磁盘告警；首次生产备份与候选恢复证据已归档。
- [x] 新增单厂商业交付验收基线，明确交付范围、环境准入、业务闭环、岗位签署、发布和回滚门禁。
- [x] 作业指导书扩展到 138 个流程，并同时生成 Markdown、DOCX、PDF 和可搜索离线 Web 版。
- [x] `v0.1.365` 将 SOP 收敛到 `sop/manifest.json` 单一内容源，新增站内帮助中心、当前页面问号抽屉、权限过滤截图接口和代码变更影响声明 CI；扩展为 140 个流程、142 页，并重生成 Markdown、DOCX、PDF 与离线 Web。
- [x] `v0.1.366` 增加安全的历史附件路径重定位和应用级恢复演练命令；使用生产候选副本验证 252 个附件文件、readiness、登录、三类业务数据与附件原文件，SOP 扩展为 141 个流程。
- [x] `v0.1.370` 快捷帮助可新开仅当前功能的全屏页，完整帮助中心也改为新页面打开；两者保留登录和服务端权限过滤，SOP 扩展为 148 个流程并重生成四种格式。
- [x] `v0.1.371` 站内帮助同步增加 6 个设备维保流程，SOP 扩展为 154 个流程并重生成 Markdown、DOCX、PDF 与离线 Web。
- [x] `v0.1.372` 站内帮助同步增加 7 个质量标准、抽样、逐项判定、附件和趋势流程，SOP 扩展为 161 个流程并重生成 Markdown、DOCX、PDF 与离线 Web。
- [x] `v0.1.373` 站内帮助同步增加 8 个来料标准、收货隔离、质量任务、判定、趋势和红冲流程，SOP 扩展为 169 个流程并重生成 Markdown、DOCX、PDF 与离线 Web。
- [x] `v0.1.374` 站内帮助同步增加来料命令授权与岗位操作边界 2 个流程，SOP 扩展为 171 个流程并重生成 Markdown、DOCX、PDF 与离线 Web。
- [x] `v0.1.375` 站内帮助同步增加物流命令授权、发货/签收/取消、退货收货/拒绝与移库确认/冲销 8 个流程，SOP 扩展为 179 个流程并重生成 Markdown、DOCX、PDF 与离线 Web。
- [x] `v0.1.376` 站内帮助同步更新班后实绩登记并增加执行上下文快照核对和例外原因 2 个流程，SOP 扩展为 181 个流程并重生成 Markdown、DOCX、PDF 与离线 Web。
- [x] `v0.1.377` 以指定提交的全部迁移执行结果审计生产物理 Schema，在不改写源候选的前提下归档 17 张退役表/1 行历史配置并生成零漂移候选；收敛后应用检查通过，但旧备份 RPO 超过 24 小时，生产切换仍阻断。
- [x] `v0.1.378` 建立 134 个写处理器审计覆盖清单和 CI 守卫，补齐 14 个高风险命令的事务审计，并同步“查看操作记录”SOP。
- [x] `v0.1.381` 将 2,803 个、约 2,106.3 MiB 的生成 DOCX/PDF/Web 从当前 Git/Docker 发布树移出，发布树降至约 58.5 MiB；新增 128 MiB 门禁、浅历史 CI、开发期 Markdown-only 和最终成品一次性校验。旧提交仍含二进制历史，彻底清理须单独维护窗口。
- [x] `v0.1.382` 更新 31 个受影响导航工作流、复核 1 个文字编码修正并只生成当前 Markdown；DOCX/PDF/离线 Web 保持最终交付时一次生成、一次完整验收，避免开发期重复检查全部文档。
- [x] `v0.1.383` 建立一个公开 HTTPS 基地址、精确版本 PDF/DOCX 路径、版本与 latest 清单、文件大小和 SHA-256 契约；完整帮助中心只在配置有效地址后显示下载入口，开发期仍不重复生成最终成品。
- [x] `v0.1.384` 增加生产镜像内的站内文档库受控登记命令：默认零写入预检，支持从本地清单或唯一 OSS 地址读取当前精确版本，强制文控权限、版本/路径/大小/SHA-256、50MB 上限和一致备份引用；以可重试草稿补齐附件，最终事务化启用当前版、归档旧版并写发布审计。
- [x] `v0.1.385` 为串行 40 项领域/治理基线增加 GitHub workflow annotation；失败时对外状态直接显示子检查名、退出码和用时，避免只留一个无法定位的 exit code 1。
- [x] `v0.1.386` 根据远端 annotation 定位来料质量 HTTP 回归中的并发登录 500；保留 5 岗位并发回归，并在单实例 SQLite 边界内串行化登录限流、会话、登录状态与审计写链路。
- [x] `v0.1.387` 根据下一条远端 annotation 定位生产数据库审计在浅克隆中缺少 `be7e18f`；只精确拉取该历史对象，避免下载完整 70MiB Git 历史包后再执行只读审计。
- [x] `v0.1.388` 将左上角 MES-lite 主模块按钮明确为统一仪表盘首页入口，桌面与移动端复用同一导航动作，MRP/ERP 继续保持禁用预留；同时新增主界面导览文档、可维护分镜与 1080p 中文旁白短视频生成命令，视频成品继续隔离在 Git/Docker 发布树之外。
- [ ] 提供真实 OSS/COS Bucket、公开下载域名和最小权限上传凭据后，执行最终 `sop:release`、上传、异地下载哈希复核，并在最新生产一致备份后由真实文控账号执行预检和登记；当前仅在全迁移临时数据库、临时附件目录和本地 HTTP OSS 模拟上验证，不能表述为生产成品已经发布。
- [ ] Coolify S3 Storage 为空，异地副本无法在缺少端点、Bucket 和凭据时真实验收；真实 Coolify 挂载切换也需要维护窗口。
- [ ] 历史 Git 数据清理和账号/密钥轮换必须单独安排维护窗口，不能与本阶段代码发布混合执行。

## 4. 暂停线

- 阶段 0 未完成前，不开放公网注册，不扩展多租户或多实例。
- 阶段 1 未完成前，不继续增加依赖 `Product` 的库存和生产字段，也不恢复旧生产入口。
- 阶段 2H 已形成生产/退货/按标准启用的来料检验、任务快照、逐项结果、附件与描述性趋势，但逐件序列号、SPC/过程能力、多级审批和真实生产权限验收完成前，不宣传为完整企业级质量管理系统。
- PostgreSQL 和对象存储迁移不能替代领域模型治理；先固定单厂业务不变量，再迁移基础设施。
