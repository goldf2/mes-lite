# MES-lite v0.1.197

发布日期：2026-08-05

## 更新内容

- 将尚未进入主线的 `feat/ai-loading-indicator` 分支合并到 `main`，使全部现有本地和远程功能分支均已包含在主线历史中。
- 合并冲突保留主线当前的可配置 AI 页面加载组件，包括服务端首屏配置、加载图标开关、紧凑布局和全屏遮罩能力。
- 补回该分支已有的 `v0.1.193` 历史发布记录，保持版本历史可追溯。

## 影响范围

- 不改变业务流程、数据模型、接口契约或权限规则。
- 不新增数据库迁移；生产运行时代码保持 `v0.1.196` 的最新实现。
- 本次仅收口 Git 分支历史和发布记录，无需更新 MiniERP 建模与流程文档。

## 验证结果

- 确认全部现有本地及远程功能分支的提交均可从 `main` 到达。
- `npm run verify:release-notes` 通过。
- `npm run build` 通过。
