# MES-lite v0.1.267

发布日期：2026-08-09

## 更新内容

- 新增统一页面注册表，集中 38 个页面的名称、类型、路由状态、权限资源、工作区入口、系统分区、打开方式和渲染键。
- 菜单分组、基础导航、工作区功能目录和页面模块定义改为从同一注册表派生，删除 `HomeApp.tsx` 中手工维护的 34 项基础菜单。
- 新增类型安全的页面渲染适配器注册表，集中 Next.js 动态加载；`WorkspacePageHost.tsx` 从 212 行缩减为 39 行。
- `HomeApp.tsx` 从上一版本的 1271 行进一步缩减为 1233 行。
- 页面模块验证增加工作区功能键唯一性、导航来源和渲染适配器完整性检查。

## 影响范围

- 不改变菜单数量、页面名称、工作区归属、权限资源、页面入口和打开方式。
- 不改变 API、Prisma 模型、业务数据或页面布局。

## 验证结果

- TypeScript 类型检查通过。
- 页面注册、配置模块、工作区导航、应用壳和响应式导航验证通过。
- Next.js 生产构建通过。
