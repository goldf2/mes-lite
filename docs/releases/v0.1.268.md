# MES-lite v0.1.268

发布日期：2026-08-09

## 更新内容

- 新增页面导航控制器，集中当前页面、物料子页、BOM 编辑目标、URL 参数同步、页面连续性和滚动位置恢复。
- 新增工作区偏好控制器，集中工作台布局读取、保存、功能键校验和使用次数上报。
- `HomeApp.tsx` 不再直接读写页面连续性存储或调用工作区偏好接口，行数从 1233 行缩减为 1045 行。
- 新增应用壳状态控制器契约验证，防止已拆分职责重新回流到应用壳。

## 影响范围

- 不改变页面入口、菜单分类、权限资源、URL 格式、页面滚动恢复、BOM 打开流程或工作台偏好行为。
- 不改变 API、Prisma 模型、业务数据和页面布局。

## 验证结果

- TypeScript 类型检查通过。
- 应用壳状态控制器、页面模块、工作区偏好、工作区导航、响应式导航和发布文档验证通过。
- Next.js 生产构建通过。
