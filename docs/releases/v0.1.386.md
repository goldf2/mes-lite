# v0.1.386 单实例 SQLite 并发登录写保护

日期：2026-08-15

## 问题现象

- GitHub Actions 运行 `31885507574` 的 40 项领域/治理基线在 `verify:incoming-quality-inspections-http` 失败。
- v0.1.385 annotation 将根因定位到 5 个岗位账号并发登录时，其中一个请求返回 HTTP 500；生产构建、应用恢复演练和此前 27 个子检查均已通过。

## 更新内容

- 在当前单实例 SQLite 部署边界内，为登录接口增加进程级写队列。
- 同一次登录的来源限流、密码校验、会话创建、登录状态更新和审计写入完整执行后，才开始下一次登录写链路。
- 保留来料质量 HTTP 验证中的 5 岗位并发登录，不把测试改为串行；失败信息额外包含账号和响应正文。

## 影响范围

- 不新增页面、接口、权限资源、Prisma 模型或迁移。
- 登录成功、失败、锁定、限流、安全 Cookie 和审计语义不变；并发登录由数据库竞争改为应用内排队。
- 该保护针对当前商业交付定义的单应用实例 SQLite 形态；未来改为多实例时应先迁移到支持并发写的服务型数据库，不能把进程内队列当作跨实例锁。
- SOP 页面步骤无变化，只生成当前版本 Markdown 同源文档。

## 验证结果

- 远端失败证据：`verify:incoming-quality-inspections-http`，并发登录 HTTP 500。
- 修复后的来料质量 HTTP 回归连续通过 3 次；身份认证服务、类型检查、Lint、临时 Node 20.19.4 的完整 40 项基线和生产构建均通过。
- 推送后仍以同一提交的 GitHub Actions 结果作为 Linux 发布门禁，不用本机结果替代远端验收。

## 回滚

本版本没有数据库迁移或业务数据变更，可将代码回退到 `v0.1.385`；但回滚会恢复班次并发登录可能返回 500 的风险。生产回滚仍须先确认当前备份和 readiness，不应回滚数据库文件。
