# v0.1.397 开发结束与人工跟进交接

日期：2026-08-17

## 更新内容

- 新增《开发结束与人工跟进事项清单》，将生产 Schema/映射、真实岗位权限、业务闭环回切、OSS 发布、异地灾备和历史安全清理拆分为 M-01 至 M-09 九项人工工作。
- 记录 `v0.1.396` 已取得的远端 CI、Coolify 部署、生产版本、健康检查、持久挂载和最新一致备份技术证据。
- 将治理总目标状态改为“开发任务已结束；生产与商业验收转交人工跟进”，保留未签字事项的风险门禁，不把人工缺口误写成已完成。
- 为每项人工工作补充优先级、负责人、前置条件、完成证据、建议顺序和空白登记区。

## 影响范围

- 仅更新版本、交付、治理、架构索引和当前 Markdown SOP 文档。
- 不改变页面、业务流程、HTTP API、权限资源、Prisma 模型、数据库迁移或现有 181 条操作步骤。
- 本次无需更新领域模型或业务流程图，因为系统行为和数据结构没有变化。

## 验证结果

- 版本号、锁文件、发布记录、文档事实基线、SOP 影响声明和当前 Markdown SOP 保持一致。
- 文档链接、发布树、SOP 目录、全量 CI 基线、类型检查、Lint 和生产构建通过。
- 未完成项目仍保持“待人工”或“部分通过”，没有用本地验证替代生产操作和企业签字。

## 回滚

本版本没有代码、配置或数据迁移。需要回退时可恢复 `v0.1.396` 的文档和版本声明；生产数据库、附件、业务流程及 v0.1.396 已部署功能均不受影响。人工跟进过程中形成的外部工单和签字记录不应随 Git 回滚删除。
