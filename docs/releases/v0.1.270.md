# MES-lite v0.1.270

发布日期：2026-08-09

## 更新内容

- 新增桌面导航控制器，集中导航显示偏好、工作区布局、左栏自动隐藏和临时展开状态。
- 将标准/双列侧栏宽度读取、保存、鼠标拖动、键盘调整和双击恢复默认值迁出应用壳。
- `HomeApp.tsx` 从 1045 行缩减为 854 行，应用壳不再直接管理桌面导航计时器、媒体查询和尺寸持久化。
- 应用壳状态契约上限从 1100 行收紧为 900 行，并增加桌面导航控制器完整性检查。

## 影响范围

- 不改变桌面导航模式、显示方式、自动隐藏时序、侧栏宽度范围、菜单内容或移动端导航。
- 不改变页面注册、权限、API、Prisma 模型、业务数据和页面布局。

## 验证结果

- TypeScript 类型检查通过。
- 应用壳状态控制器、工作区布局、页面模块、工作区导航和响应式导航验证通过。
- Next.js 生产构建通过。
