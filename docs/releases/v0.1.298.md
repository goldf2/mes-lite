# MES-lite v0.1.298

日期：2026-08-09

## 更新内容

- 将派工单、可派工工单、客户、工序、状态和优先级契约迁入 `modules/production`。
- 新增派工领域 HTTP client，统一筛选查询、候选读取、新建和状态流转；派工页面不再直接调用 `fetch`。
- 派工页从根级 `app/components` 迁入生产领域，并从 730 行降至 646 行；根级存量领域页从 13 个降至 12 个。
- 页面注册改为通过生产模块公开出口加载派工页，并新增模块边界验证。

## 影响范围

- 保留派工卡片/列表、筛选、详情、新建、状态流转、附件、AI 凭据识别、创建后 PDF 归档和补打行为。
- 不修改 Prisma Schema、API 契约、派工业务数据或服务器正式数据。

## 验证结果

- 派工模块、业务单据打印、原始凭据、附件管理、模块边界、页面模块、版本文档和开发文档校验通过。
- TypeScript 类型检查与 Next.js 生产构建通过；构建仅保留项目既有警告。

## 数据安全

- 本机 SQLite 中只保留一条明确标记的测试记录；数据库文件和构建缓存不纳入提交。
- 正式数据仍以服务器数据库为准，本次没有连接、读取或写入服务器正式库。
