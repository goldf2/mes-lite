# MES-lite v0.1.293

日期：2026-08-09

## 更新内容

- 将 BOM 请求 Schema、公共查询选择集、资料查询和方案保存事务迁入 `modules/bom`。
- 提取唯一主产出、包装单产出、投入/产出去重与互斥规则，以及默认版本递增纯函数，并加入回归样例。
- `app/api/boms/route.ts` 从 464 行降至 45 行，系统超过 300 行的存量 API 从 6 个降至 4 个。
- 扩展物料/BOM 模块守卫，限制路由与服务规模并阻止查询、换算和事务规则回流 HTTP 层。

## 影响范围

- 保留 `/api/boms` 的 GET、PUT 方法、响应结构、权限资源、审计字段和错误状态。
- 不修改 Prisma 模型、数据库迁移、现有 BOM 或服务器正式数据。

## 验证结果

- 物料/BOM 模块、模块边界、高级搜索、页面注册、版本文档和开发文档校验通过。
- TypeScript 类型检查与 Next.js 生产构建通过；构建仅保留项目既有的图片与 Hook 依赖警告。

## 数据安全

- 本机 SQLite 中只保留一条明确标记的测试记录；数据库文件和构建缓存不纳入提交。
- 正式数据仍以服务器数据库为准，本次没有连接、读取或写入服务器正式库。
