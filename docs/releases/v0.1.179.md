# MES-lite v0.1.179

发布日期：2026-08-04

## 更新内容

- 在项目级 `AGENTS.md` 中新增 Docker 与 Coolify 构建强制要求。
- 固化国内 Debian/npm 镜像、Docker Hub daemon 加速、BuildKit 缓存、有限重试与超时等规则。
- 要求慢构建步骤位于业务源码之前的稳定分层，避免普通修改反复下载依赖。
- 更新 Coolify 部署文档，消除“不再执行 apt-get”与当前 Poppler 依赖之间的矛盾。

## 影响范围

- 影响后续开发、Dockerfile 修改、Coolify 部署与故障排查方式。
- 不改变应用业务流程、数据模型或运行时功能。

## 验证结果

- 项目规则与 Coolify 部署文档已交叉校对。
- 发布版本、锁文件版本与发布索引保持一致。
- 版本文档校验与生产构建通过。
