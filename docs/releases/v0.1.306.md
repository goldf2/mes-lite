# MES-lite v0.1.306

日期：2026-08-09

## 更新内容

- 将扫码打印和锯切成本从根组件目录迁入 `modules/operations-tools`，根级 `*Page.tsx` 只保留 31 行的系统兼容分派层。
- 为扫码会话、标签打印和锯切成本新增独立 contracts、HTTP client 与视图/计算模型，两个页面均不再直接调用 `fetch`。
- 将通用标签和锯切成本保存面板从协调页提取为独立用户任务组件。
- 将材料利用率、锯缝损耗、班次产能、混合规模、负荷和保存快照输入提取为可测试纯函数。

## 影响范围

- 扫码打印页面从 553 行降至 415 行，锯切成本页面从 558 行降至 363 行。
- 保留 Honeywell 1900 扫码、PC310T 标签介质、浏览器打印、成本对象保存和方案对比现有交互。
- 不修改 Prisma Schema、API 路径或服务器正式数据。

## 验证结果

- 扫码打印、锯切成本、页面模块和模块边界专项校验通过。
- 锯切默认材料、班次、规模、物料候选合并及保存输入确定样例通过。
- TypeScript 类型检查与 Next.js 生产构建通过。

## 数据安全

- 本机 SQLite 中只保留明确标记的测试记录；数据库文件和构建缓存不纳入提交。
- 正式数据仍以服务器数据库为准，本次没有连接、读取或写入服务器正式库。
