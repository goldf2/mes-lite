# v0.1.464 — v0.1.463 生产验收证据与交接收口

日期：2026-09-06

## 更新内容

- 补齐 v0.1.463 从候选到生产的可复核证据：候选 CI、main CI、Coolify 部署、容器健康检查、迁移、备份和公开 readiness。
- 同步当前状态、下一步任务、发布记录和会话日志，避免把已完成的生产部署继续记录为待验证。

## 影响范围

本版本仅更新版本与研发/运维交接文档，不改变业务代码、数据库结构、库存数量、BOM、工艺路线、成本规则、权限或页面操作路径。v0.1.463 的 Docker readiness 120 秒启动窗口和迁移前备份配置继续生效。

## 验证结果

- 精确提交 `72beb984f58c46f98589568ff167eca94d290b2a` 的候选 CI `33991640324` 和 main CI `33992378118` 均为 success。
- Con01 Coolify 部署 `q7vtzftk9wah6r6bu4tv2nga` finished；新容器镜像和 `SOURCE_COMMIT` 均为 `72beb984f58c46f98589568ff167eca94d290b2a`，旧容器已退出。
- 新容器环境变量 `MES_LITE_PRE_MIGRATION_BACKUP_ENABLED=true`，Docker 健康检查 `StartPeriod=2m0s`、间隔 10 秒、重试 6 次，最终状态 healthy。
- 启动日志显示备份归档创建成功（219 条附件记录、664 个文件、数据库 quick check 为 ok），随后 96 条 Prisma 迁移无待应用，Next.js Ready。
- 备份归档为 `mes-lite-backup-2026-09-05T21-26-19-049Z-c67455c7.tar.gz`，SHA-256 为 `575a44306e32fa31fc59981c84d825567074c1b36e9ffa866a99442812e8867e`；服务器执行 `sha256sum -c` 返回 OK。
- 公网 `/api/health` 与 `/api/health/ready` 返回 200；ready 检查中 database、migrations（96）、dataStorage、attachmentStorage、backupFreshness、cadPreview 均 pass，仅 Collabora 未配置为既有 warn。
- 持久卷仍为 `/opt/mes-lite/data -> /app/data`、`/opt/mes-lite/uploads -> /app/public/uploads`、`/opt/mes-lite/backups -> /app/backups`，未删除或重建业务数据卷。

## 数据与回滚

本版本没有迁移和数据改写。若后续滚动更新失败，继续以 Coolify 自动回滚为边界，不删除旧镜像、数据库或持久卷；任何生产数据修复仍需独立备份、预览、确认和审计。
