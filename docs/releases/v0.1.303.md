# MES-lite v0.1.303

日期：2026-08-09

## 更新内容

- 将流程转移页从根组件目录迁入 `modules/production`，并通过生产模块公开入口加载。
- 新增流程转移 client、契约和视图模型，统一列表及候选项、草稿保存、确认、冲销请求与状态展示。
- 流程转移协调页从 591 行降至 485 行且不再直接调用 `fetch`；根级存量页面从 6 个降至 5 个。
- 扩展流程转移守卫，同时验证前端模块边界与库存事务不变式。

## 影响范围

- 保留流程转移的列表/卡片、精确搜索、物料图片、员工选择、PDF、确认和冲销交互。
- 不修改 Prisma Schema、API 路径、库存计算规则或服务器正式数据。

## 验证结果

- 流程转移库存事务、页面模块、模块边界、高级搜索和业务单据打印专项校验通过。
- TypeScript 类型检查与 Next.js 生产构建通过。

## 数据安全

- 本机 SQLite 中只保留一条明确标记的测试记录；数据库文件和构建缓存不纳入提交。
- 正式数据仍以服务器数据库为准，本次没有连接、读取或写入服务器正式库。
