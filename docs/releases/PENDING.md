# 待整理版本记录

本页保留尚未整理成独立发布说明的小版本变更。阶段收口时再按主题合并、校对并转入正式发布文档；在此之前不得删除未迁移的记录。

| 版本 | 日期 | 变更记录 | 验证证据 | 待整理文档 |
| --- | --- | --- | --- | --- |
| v0.1.458 | 2026-09-04 | 修正物料全景左右主从布局的三个边界：左栏最大占比由 48% 收紧为 42%，档案与库存到超宽屏才内部并排；隐藏当前展开模块时同步清空活动状态；公共 `SplitWorkspace` 在浏览器站点存储读写异常时回退到默认或当前会话分栏 | 定向回归、TypeScript、研发文档、版本说明、SOP、架构/模块边界与差异格式通过；1280px 最大分栏、隐藏/恢复模块和 390px 无横向溢出通过真实浏览器验证；候选 CI 待运行 | 已同步 SOP 影响和项目交接文档；无接口、数据库、权限或业务计算变化 |
| v0.1.457 | 2026-09-04 | 物料全景升级为左纵览、右明细的主从仪表台：宽屏复用公共可调宽 `SplitWorkspace`，窄屏自动上下排列；右侧明细默认全部收起且一次只展开一组，切换时自动收起上一组。模块显示、顺序、密度以及库存、BOM、锯切成本等原数据逻辑保持不变 | 物料/BOM 模块边界、TypeScript、定向 Lint、本地窄屏真实交互和候选 CI `33883741692` 已通过；精确 SHA `6e1491915c034ec05bfbcca996bc37304c2d5082` 已推入 `main`，主线 CI `33886361407` 运行中，生产宽屏验收待完成 | 已同步桌面/移动/响应式规范、前端组件复用审查和项目交接文档；无接口、数据库、权限或业务计算变化 |
| v0.1.456 | 2026-09-04 | 物料全景由七组信息同时平铺改为仪表台：首屏固定展示可用库存、待检/冻结、BOM 关系、已存单位材料成本和库存单价；详细资料按模块折叠，默认只展开档案与库存，并支持全部展开/收起。布局设置继续保留模块显示、顺序和密度，不再提供失去意义的卡片宽度设置 | 完整 50 项本地 CI 基线、生产构建、TypeScript、定向 Lint、SOP、版本说明和差异格式验证通过；候选精确 SHA CI 和生产页面待验证 | 同步桌面/移动界面规范、前端组件审查和项目交接文档；无接口、数据库、权限或业务计算变化 |
| v0.1.454 | 2026-09-04 | 锯切测算保存入口默认关联已有物料；保存后形成可复用锯切成本对象，并可选关联到目标物料的草稿 BOM。物料全景新增按锯切方案的物料关系查找成本对象，未加入 BOM 的已绑定锯切成本也能展示；已发布 BOM 仍保持只读 | 锯切成本模块、成本领域事务、物料全景查询、物料/BOM 模块、TypeScript、定向 Lint、发布说明和差异格式验证通过；候选 CI 待完成 | 同步数据模型、功能模型、系统流程与项目交接文档；无数据库迁移或权限变化 |
| v0.1.453 | 2026-09-02 | 将误放在通用 `lib` 的库存事务引擎收归库存模块公开出口，并将库位转移拆为独立服务；来料、生产和销售调用方统一经过 `@/modules/inventory`，旧 `lib/inventory.ts` 仅保留兼容出口；715 行核心巨型文件降为 601/130 行两个稳定职责，超过 700 行的核心文件清零；跨模块依赖与循环未增长 | TypeScript、Lint、架构量化与模块边界；来料、多单位/实测、生产实绩、快捷生产、库位转移、发货退货/冲销/多明细和库存事务账定向回归；完整 49 项领域/治理基线与精确候选 CI | 已同步代码架构量化基线和模块边界；不改变业务流程、接口、数据模型、权限或界面，无需更新 minierp 建模/流程文档 |
| v0.1.452 | 2026-09-02 | 库存发货与退货批次职责继续内聚：发货 FIFO 分配、发货冲销、历史发货兼容和退货回原发货批次进入独立模块内部服务；库存公开出口、销售调用方和事务行为保持不变；`inventory-status-service.ts` 由 795 行降至 556 行，超过 700 行的核心文件由 2 个降至 1 个 | TypeScript、Lint、架构量化与模块边界；发货退货、发货冲销、多明细发货、销售模块、批次谱系和库存事务账定向回归；完整 49 项领域/治理基线与精确候选 CI | 已同步代码架构量化基线和模块边界；不改变业务流程、接口、数据模型、权限或界面，无需更新 minierp 建模/流程文档 |
| v0.1.451 | 2026-09-02 | 库存状态巨型服务按稳定职责拆分：收货建批、历史库存兼容、可用批次 FIFO 消耗/恢复和生产投入批次分配进入独立模块内部服务；公开导出、调用方和事务行为保持不变；架构递减预算由 1,237 行收紧到 795 行 | TypeScript、架构量化与模块边界；生产消耗/实绩、批次谱系、发货退货/冲销、库存事务账和库存流水定向回归 | 已同步代码架构量化基线；不改变业务流程、接口、数据模型、权限或界面，无需更新 minierp 建模/流程文档 |
| v0.1.447 | 2026-08-29 | 仓库全景普通画布增高并新增视口全屏；全屏保留搜索、缩放、平移、库位详情。库位详情可从有可用量的物料直接发起流程转移，复用统一录入弹窗、转移单创建/确认接口、权限、批次和库存事务；确认失败明确保留草稿 | 仓库数字孪生投影/界面边界、流程转移完整事务与权限、TypeScript、Lint、模块边界、SOP 和正式候选 CI | 已同步系统交互、桌面/移动规范、功能流程、页面权限接口矩阵和仓库全景 SOP；无数据库结构或新接口 |
| v0.1.446 | 2026-08-29 | 数据工具的一致性检查新增“导出故障明细”；把当前检查结果导出为 Excel 可直接打开的 UTF-8 CSV，包含严重程度、故障类型、对象标识、当前/期望值、可执行操作和完整详情，并防止文本触发电子表格公式 | 故障明细字段、中文 BOM、CSV 特殊字符与公式转义、文件名、SOP、TypeScript、Lint、模块边界和正式候选 CI | 已同步系统流程、功能页面权限接口矩阵和数据工具 SOP；无数据库结构、接口或权限变化 |
| v0.1.441 | 2026-08-27 | 修复多明细发货单把前端临时键提交到严格接口而触发“参数错误”；快捷生产日报移除重复的计划主产出数量字段，BOM 数量直接进入可编辑实际行，主产出原位修改按比例更新未手改行；投入/产出逐行显示权限范围库存参考 | 销售模块、快捷生产完整事务/质量/冲销/库存范围回归、TypeScript 与定向 Lint | 同步当前流程、领域/功能/结构审查、界面规范、专项操作和 SOP 源文件 |
| v0.1.440 | 2026-08-27 | 快捷生产日报按“计划产出 → BOM → 计划主产出数量”同比例预填全部投入和产出；操作员复核实际值后原子过账；页面新增不可覆盖的生产日报流水与有原因冲销，冲销追加反向库存流水并保留原日报 | 快捷生产完整事务、质量待检与放行、正反向库存流水双向关联、重复冲销拦截、独立冲销权限、数据范围、审计、TypeScript、Lint 与相关领域回归 | 同步 ADR、系统流程、结构审查、专项操作与全流程指导书 |
| v0.1.439 | 2026-08-25 | 快捷生产日报优先按计划产出筛选已发布 BOM，同时保留已有投入物料辅助筛选；任一条件可独立使用，两项并用时取交集；选择副产出仍带出整套复杂 BOM 及其真实主产出 | 产出/投入/交集纯规则、复杂 BOM 主副产出回归、快捷生产完整事务、TypeScript、Lint、SOP 与正式候选 CI | 同步功能模型、结构审查、快捷过账专项操作与全流程指导书 |
| v0.1.438 | 2026-08-24 | 快捷生产日报同步采用 BOM 可选预设；允许无 BOM 临时生产/转换，并以公共一对多骨架登记多投入、多产出及各自库位 | 临时完整 SQLite 覆盖 BOM 多产出、无 BOM、直接/待检、放行、冲销和失败回滚；TypeScript、Lint、迁移与正式候选 CI | 已整理为 `v0.1.438.md`，同步 ADR、流程和数据模型 |
| v0.1.437 | 2026-08-24 | 生产订单将 BOM 从强制约束改为可选标准预设；允许无 BOM 临时生产/转换，并允许班后实绩按现场实际增删输入和输出 | 生产订单与实绩临时完整 SQLite 回归、TypeScript、Lint、模块边界、相关生产/质量/批次回归与正式候选 CI | 已整理为 `v0.1.437.md`，同步 ADR、领域/功能/数据/流程/交互/模块边界和公开 ER 说明 |
| v0.1.436 | 2026-08-24 | 新增中文 BOM 数据结构改造前后对比 ER 图；按当前 Prisma Schema 展示现状，并明确区分尚未实施的目标结构、待删除的投入到产出物料歧义关系、待新增的工艺与订单快照 | HTML 自包含检查、桌面端和 390px 窄屏浏览器实测、差异格式与发布说明检查 | 独立说明页 `public/bom-er-comparison.html`；本次只新增设计说明，不修改数据库结构或业务流程 |
| v0.1.435 | 2026-08-24 | 生产日报由“主产出 → BOM”改为“投入物料 → 正式 BOM／主产出”；候选显示主产出、方案和版本，唯一候选自动选中，选定后仍展开并过账 BOM 全部投入 | 投入反查纯规则、临时完整 SQLite 快捷日报事务、TypeScript、Lint 与正式候选 CI | 已整理为 `v0.1.435.md`，同步交互规范、功能/流程模型、建模审查和模块边界 |
| v0.1.434 | 2026-08-22 | 库存盘点从生产日报拆出并归入库存菜单；生产日报只保留 BOM 快捷转换；页面与接口提示统一库存盘点语义 | 页面注册、导航配置、库存盘点事务、生产日报、SOP、TypeScript、Lint 与正式候选 CI | 已整理为 `v0.1.434.md`，同步页面矩阵、开发手册和 SOP |
| v0.1.433 | 2026-08-22 | 生产日报默认增加正式 BOM 快捷转换；按实际主产出展开理论投入并允许修正实耗；产出可直接可用或进入待检并生成质量任务；单事务扣料、产出入库、成本结转和审计；保留库存盘点校准 | 临时完整 SQLite BOM 转换、直接/待检入库、后续质检放行、待检日报/质量任务/批次联合冲销、无订单派工和失败回滚回归；旧日报和盘点回归、TypeScript、差异格式；候选 CI 待推送后核对 | 已整理为 `v0.1.433.md`，同步功能/领域模型、权限矩阵、结构/时序、模块边界和专项操作文档 |
| v0.1.432 | 2026-08-22 | 增加生产日报每日盘点快捷入口；一个库位、多物品实盘数整单原子校准；跳过账实一致行；不创建生产或质量记录并保留 FIFO/受控库存保护 | 临时完整 SQLite 事务回归、页面/工作台/API/模块边界验证、TypeScript、ESLint；正式候选 CI 与部署验证待发布门禁完成 | 已整理为 `v0.1.432.md`，同步 ADR、权限矩阵、系统结构、开发文档、专项操作与全流程指导书 |
| v0.1.431 | 2026-08-22 | 销售订单与发货单完全解耦；发货单支持一个客户、多条实际发货物料；客户物料未发/超发动态参考进入发货录入与仪表盘；销售/发货列表增加物料图片和用户可选显示项；产品文档移除工作中心；业务弹窗默认禁止点击遮罩关闭 | 全新 SQLite 迁移、销售/发货/货箱/退货/打印/文档/生产上下文/仪表盘回归，TypeScript、ESLint、模块边界、数据完整性、动态搜索字段、SOP、开发文档、发布说明及本地浏览器交互 | 已整理为 `v0.1.431.md`；候选 GitHub Actions 与生产部署后版本/健康/页面验证待发布门禁完成 |
| v0.1.430 | 2026-08-21 | CAD 预览升级为可切换的多引擎链路：默认镜像内置 LibreDWG 与 MIT 许可 ACadSharp，支持部署方合法外挂 QCAD Professional；系统设置新增“文件预览”，显示各引擎健康状态并选择自动或指定引擎。自动模式按配置顺序回退并拒绝明显空白/稀疏的假成功结果，派生 PDF 与缩略图按引擎隔离缓存 | TypeScript、CAD 客户端/服务结构、系统设置模块、页面注册、权限迁移与工作区导航定向回归；候选 GitHub Actions 负责构建并运行多引擎 Docker 冒烟及完整 `verify:ci` | 已同步多引擎服务说明、ADR、Coolify 总册与专项手册、系统交互/流程/数据模型、页面模块和权限矩阵；生产仍需重新部署 `cad-preview` 与 MES-lite，并用真实企业图纸逐引擎验收 |
| v0.1.429 | 2026-08-21 | 修复产品文档卡片缩略图长期停留在“正在读取缩略图”：原生懒加载图片不再在完成前使用 `display:none`，改为保留布局的透明图片与覆盖式加载/错误状态，避免浏览器因元素不可见而不发起请求 | `verify:libredwg-cad-preview` 先复现失败，修复后锁定懒加载图片不得隐藏且必须保留透明布局；TypeScript、Lint 与文档 SOP 校验 | 生产部署后确认卡片缩略图请求实际发出，成功显示图片、失败进入“预览不可用” |
| v0.1.428 | 2026-08-21 | 统一外挂 CAD 字体卷的 Coolify 标准模式：卷在容器平台层保持可写，仅供 root 入口幂等修权；入口完成后转换进程仍为只读。修正专项手册遗留的“只读持久挂载”冲突，并在 Custom Docker Options 示例中完整保留启动修权和降权所需的六项 capability | `verify:libredwg-cad-preview` 新增挂载模式与 capability 完整性回归；完整 `verify:ci` | 生产 `cad-preview` 仍需核对新镜像、RW 字体卷、启动日志和 `/health.fontDirectories` |
| v0.1.427 | 2026-08-21 | 外挂 CAD 字体目录采用受控 root 初始化、随后降权的标准模式：入口仅修复 `/opt/cad-fonts` 白名单，拒绝符号链接，统一为目录 `root:cadpreview 0750`、文件 `root:cadpreview 0640`，再清空 capability 集并以 UID 10001 运行转换器；启动与受令牌保护的 `/health` 同时报告路径所有者、UID/GID、权限位和有效读写状态，配置路径不可用时明确拒绝启动 | 非 root 真实入口冒烟覆盖可读目录、普通文件和无权限目录；`verify:libredwg-cad-preview` 锁定路径白名单、权限修复、capability 清空、健康诊断；Python 编译、Docker 镜像转换冒烟及完整 `verify:ci` | 已同步 CAD 服务说明、Coolify 专项手册、部署总册、ADR 与文档 SOP；生产部署后核对入口日志和 `/health.fontDirectories`，再上传授权字体复验 |
| v0.1.426 | 2026-08-21 | 修正文档库标题逐字换行和编辑侧栏过窄；公共可搜索选择器箭头升级为可展开/收起按钮。批量导入后列表缩略图改为延迟加载，MES 与 `cad-preview` 活动转换默认均限为 2 个；转换器支持 `/opt/cad-fonts` 外挂企业字体目录并在启动时重建 ezdxf 缓存，缺失大字体回退时清除失效引用 | 文档布局/下拉结构门禁；`verify:cad-preview` 覆盖五图并发不超过 2；`verify:libredwg-cad-preview` 锁定外挂字体、缓存重建、bigfont 回退和缩略图延迟加载；TypeScript、Lint 与完整 `verify:ci` | 已同步桌面、响应式、系统交互、CAD Coolify 手册和文档 SOP；生产部署后复核页面布局、代表性下拉、29 图纸分批预览及外挂字体真实样本 |
| v0.1.424 | 2026-08-21 | 修复产品文档编辑详情左侧表单被压窄后中文标签逐字换行；建立资源实际字段目录，智能关键词与高级搜索共同从同一目录派生，产品文档按类别动态合并扩展字段；移除旧映射式高级搜索组件，并为设备点检、设备维保、质量任务、检验标准和批次追溯补齐公共高级搜索 | `verify:advanced-search-fields` 覆盖 29 个搜索页面、字段目录派生、服务端字段白名单和旧映射消费者清零；设备、质量、批次、文档领域回归、TypeScript、Lint 与完整 `verify:ci` | 已同步界面规则、响应式验收、结构审查、模块边界与 SOP 影响；生产部署后复核文档详情 1024/1440 宽度和代表性动态搜索字段 |
| v0.1.423 | 2026-08-21 | 产品文档“字段设置”为每个扩展字段增加编辑入口；未使用字段可修改名称、类型和下拉选项，已使用字段仍可安全改名但锁定类型与选项；入口复用文档类别修改权限，删除保护保持不变 | `verify:document-metadata` 覆盖未使用字段完整修改、已使用字段改名、类型修改拒绝及同类别重名拒绝；`verify:document-server`、`verify:document-categories`、`verify:sop`、TypeScript、定向 Lint（0 错误、3 个存量 Hook 警告）和本地 45 项 `verify:ci` 通过 | 已同步字段规则、桌面/移动/响应式、权限矩阵和 SOP；后续集中整理正式发布说明 |
| v0.1.421 | 2026-08-20 | 修复 CAD 预览重新生成后刷新文档库仍可能恢复旧封面的问题；附件新增持久化 `previewRevision`，转换和缩略图全部成功后递增，列表封面与全屏预览 URL 均携带该修订号，从请求地址层面绕开浏览器或代理的旧缓存 | `verify:attachment-management` 回归先复现失败、修复后通过；`verify:cad-preview`、`verify:document-server`、Prisma 全新库迁移与 TypeScript 通过 | 新增非破坏性附件预览修订字段；不改变操作流程 |
| v0.1.420 | 2026-08-20 | 修复 CAD 重新生成后封面已更新、全屏预览仍命中旧 PDF 的问题；CAD 全屏预览响应改为按派生 PDF 大小与修改时间生成 ETag，并要求浏览器每次重新验证缓存，转换结果变化后立即返回新文件 | `verify:attachment-management` 回归先复现失败、修复后通过；`verify:cad-preview`、`verify:document-server` 与 TypeScript 通过 | 无流程或数据模型变化 |
| v0.1.419 | 2026-08-20 | 修复产品文档重新生成 CAD 预览后，详情与全屏已更新但文档库列表封面仍保留旧缩略图的问题；重建成功后同步刷新列表主附件和当前文档摘要的缓存版本，CAD 缩略图再次打开时按文件版本重新验证 | `verify:document-server` 与 `verify:attachment-management` 回归均先复现失败、修复后通过；TypeScript、定向 Lint（0 错误、3 个存量 Hook 警告）、本地 44 项 `verify:ci` 通过 | 无流程或数据模型变化 |
| v0.1.418 | 2026-08-20 | CAD 转换器在图纸引用的 SHX 或大字体缺失时改用内置 Noto 中文字体，避免 DWG/DXF 中文显示为方框；CAD PDF 与缩略图缓存升级为 v2，使既有图纸在首次重开时自动生成修复后的预览；公共附件管理与产品文档详情增加“重新生成预览”，只重建当前 CAD 派生文件并保留原图、权限校验和审计 | `verify:cad-preview`、`verify:libredwg-cad-preview`、`verify:attachment-management`、`verify:document-server`、Python 编译检查、TypeScript、定向 Lint；使用生产真实 `7122.dwg` 临时转换，中文标题栏、材料、倒角和公司名称均可见，原图仍需在新镜像部署后复验 | 后续正式发布说明集中整理 |
| v0.1.417 | 2026-08-20 | 来料单编辑保存发生网络响应中断时，停止展示浏览器原始 `Failed to fetch`；只读回查同一单据并逐项核对头部与明细，一致时按保存成功恢复，不一致时保留草稿并提示稍后重试，且不自动重发写请求 | `verify:receiving-module`（含响应丢失恢复、内容不一致和只读不重发断言）、TypeScript、定向 Lint；本地真实浏览器已验证正常保存停留编辑窗口，以及断网提示中文、草稿保留且不泄漏原始英文异常，生产环境待部署后复验 | 后续正式发布说明集中整理 |
| v0.1.413 | 2026-08-20 | 产品文档列表将重复的“在线阅读”改为“全屏预览”并与“详情”拆分；全屏预览优先打开在线正文，无正文时打开首个附件，并支持在正文和全部附件间连续切换 | `verify:document-server`、`verify:fullscreen-dialogs`、`verify:sop`、TypeScript、定向 Lint | 后续正式发布说明集中整理 |
| v0.1.412 | 2026-08-20 | DWG/DXF 接入统一附件查看器；首次打开经隔离内部 CAD 服务生成只读 PDF 和缩略图，原文件永久保留并缓存派生结果；未配置或离线时降级为下载且不影响主业务 readiness | `verify:cad-preview`、`verify:attachment-file-types`、`verify:attachment-management`、`verify:fullscreen-dialogs`、TypeScript、定向 Lint；内部协议使用模拟转换服务验证，真实 ODA 授权服务与真实图纸版式待部署后验收 | 后续正式发布说明集中整理 |
| v0.1.411 | 2026-08-19 | XLS/XLSX/ODS 默认通过自托管 Collabora（LibreOffice 核心）和只读 WOPI 直接浏览，保留原生工作表标签与滚动；会话使用摘要令牌、proof key 和实时业务权限校验，失败时可切换兼容 PDF 或下载原文件 | `verify:wopi-viewer`、`verify:wopi-http`、`verify:attachment-file-types`、`verify:attachment-management`、`verify:fullscreen-dialogs`、`verify:sop`、Prisma 全新库迁移、TypeScript、定向 Lint；本地浏览器已验证服务不可用降级和 24 工作表兼容 PDF，真实 Collabora 容器直览待隔离服务部署后验收 | 后续正式发布说明集中整理 |
| v0.1.410 | 2026-08-19 | 产品文档库及公共附件查看器支持按名称切换 XLS/XLSX/ODS 工作表；一个工作表跨多页时只渲染该工作表页范围，目录缺失时降级为逐页选择 | `verify:attachment-file-types`、`verify:attachment-management`、`verify:fullscreen-dialogs`、`verify:sop`、TypeScript、定向 Lint；真实 24 工作表/50 页与 39 工作表/39 页 XLSX 目录映射；桌面与 390px 手机浏览器交互实测 | 后续正式发布说明集中整理 |
| v0.1.408 | 2026-08-18 | 待收货来料单把“保存整单”与“关闭”分离；保存成功后留在当前编辑窗口并可继续修改其他明细，新建成功仍正常退出 | `verify:receiving-module`、TypeScript、定向 Lint、生产页面交互实测 | 后续发布说明与来料编辑 SOP 集中整理 |
| v0.1.407 | 2026-08-18 | 七类业务单据保存与 PDF 打印分离；企业与业务规则增加 A4 紧凑/标准密度及页边距设置，默认紧凑版支持 14 条来料明细单页，打印设置变化后按新格式保留归档版本 | `verify:business-document-print`、`verify:dispatch-module`、TypeScript、PDF 页数与渲染检查 | 后续发布说明与业务单据打印 SOP 集中整理 |
| v0.1.406 | 2026-08-18 | 来料单右侧已加入明细增加编辑操作，可回填数量、辅助实测、计价、批次和库位，保存时按原项替换并支持取消编辑 | `verify:receiving-module`、TypeScript、定向 Lint、生产页面交互实测 | 后续发布说明集中整理 |
| v0.1.405 | 2026-08-18 | 全局成功/错误提示横幅提升到顶层，保留默认磨砂背景；显示设置补充最上层说明 | `verify:page-modules`、TypeScript、定向 Lint；实测 `blur(4px)` 遮罩层 `200`、提示层 `400` | 后续发布说明与显示设置 SOP 集中整理 |
