# v0.1.465 — 最终活跃容器与生产证据收口

日期：2026-09-06

## 更新内容

- 固化 v0.1.464 main 推进后 Coolify 两次同 SHA 滚动更新的最终状态，避免交接记录停留在已被替换的首个容器。
- 记录最终活跃容器、最新迁移前备份、SHA-256 校验、公开 readiness 和持久卷映射。

## 影响范围

本版本仅更新版本与研发/运维交接文档，不改变业务代码、数据库结构、库存数量、BOM、工艺路线、成本规则、权限或页面操作路径。v0.1.463 的 120 秒 readiness 启动窗口和迁移前备份配置继续生效。

## 验证结果

- 精确提交 `ea126da453e6fa8640734da1a9248d2c61bfacdd` 的候选 CI `33993847398` 和 main CI `33994513463` 均为 success。
- Con01 Coolify 部署 `x10zu5ki8won9u8pq45ecexg`、`omufsu03hc2k9l7iy9crpi14` 和文档版本自动更新队列 `eotbyz193mldkvnkb1a89jjv` 均 finished；最终活跃容器为 `jhjbwdumt46lsy4x2zj03llo-223851761552`，镜像和 `SOURCE_COMMIT` 均为 `c099394574c4cb59d8a60b62bc93701e7686f286`，状态 healthy。
- 最终容器环境变量 `MES_LITE_PRE_MIGRATION_BACKUP_ENABLED=true`，Docker 健康检查 `StartPeriod=2m0s`、间隔 10 秒、重试 6 次。
- 最终启动日志显示备份归档成功（219 条附件记录、664 个文件、数据库 quick check 为 ok），96 条 Prisma 迁移无待应用，Next.js Ready。
- 最终备份归档为 `mes-lite-backup-2026-09-05T23-10-50-652Z-934d9d76.tar.gz`，SHA-256 为 `8b57047ddb42a60d78db370a264db4bae647c150f0a7f9e2c1e1f88564f57343`；服务器执行 `sha256sum -c` 返回 OK。
- 公网 `/api/health` 与 `/api/health/ready` 返回 200；ready 检查中 database、migrations（96）、dataStorage、attachmentStorage、backupFreshness、cadPreview 均 pass，仅 Collabora 未配置为既有 warn。
- 最终仅保留一个运行中的 MES-lite 容器；持久卷仍为 `/opt/mes-lite/data -> /app/data`、`/opt/mes-lite/uploads -> /app/public/uploads`、`/opt/mes-lite/backups -> /app/backups`。

## 数据与回滚

本版本没有迁移和数据改写。若后续滚动更新失败，继续以 Coolify 自动回滚为边界，不删除旧镜像、数据库或持久卷；任何生产数据修复仍需独立备份、预览、确认和审计。
