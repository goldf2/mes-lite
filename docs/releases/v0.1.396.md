# v0.1.396 开发冻结与生产验收交接

日期：2026-08-17

## 更新内容

- 新增开发冻结与生产验收交接清单，明确“仓库开发冻结候选”“生产上线完成”和“全局治理完成”三个不同状态。
- 将治理计划中分散、重复的外部待办收敛为 G0–G6 七个唯一门禁：仓库冻结、最新生产备份、Schema/模型收敛、真实岗位权限、Coolify 部署回切、OSS 文档视频发布、异地灾备与历史安全清理。
- 为每个门禁指定负责人、必须证据和失败停止线，并给出不可交换的生产维护窗口执行顺序。
- 明确 OEE、SPC、专业 WMS、完整 MRP/APS、财务和多租户仍属未来范围，不阻挡当前轻量 MES 代码冻结，也不得表述为已经交付。

## 影响范围

- 仅更新交付、治理、架构索引、版本和当前 Markdown SOP 文档。
- 不改变页面、业务流程、HTTP API、权限资源、Prisma 模型、数据库迁移或现有 181 条操作步骤。

## 验证结果

- 当前仓库版本、锁文件、发布记录、文档基线和 SOP 影响声明保持一致。
- 文档链接、发布树、SOP 目录、类型检查、Lint 和生产构建通过。
- G1–G6 仍需真实生产环境、企业签字、对象存储或维护窗口证据；本版本不把这些外部事项标记为完成。

## 回滚

本版本没有数据迁移。可回退新增交接清单和版本文档；业务数据、在线功能及已有生产部署不受影响。
