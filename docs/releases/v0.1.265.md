# MES-lite v0.1.265

发布日期：2026-08-09

## 更新内容

- 从 `HomeApp.tsx` 提取公共账号菜单、导航字符图标和工作区页面宿主，建立正式的 `app/components/shell` 边界。
- 将业务页面的动态加载和挂载集中到 `WorkspacePageHost`，为下一阶段统一页面注册表做好准备。
- `HomeApp.tsx` 从 1532 行减少到 1271 行，继续只保留应用级导航、布局和全局状态编排。
- 更新账号菜单、配置模块和工作区布局验证脚本，使验证目标与新的 shell 边界和公共导航类型一致。

## 影响范围

- 不改变页面入口、菜单数据、权限判断、业务流程、API 或数据库模型。
- 本版本是应用壳第一阶段拆分；页面注册仍暂由 `WorkspacePageHost` 集中管理，后续再迁移为单一配置注册表。

## 验证结果

- TypeScript 类型检查通过。
- 账号菜单、配置模块、页面模块、工作区布局、响应式导航、全屏弹窗、工作区导航配置和关联文档弹窗验证通过。
- Next.js 生产构建通过。
