# MES-lite v0.1.305

日期：2026-08-09

## 更新内容

- 将员工档案页从根组件目录迁入 `modules/configuration`，明确员工属于业务配置而非账号权限。
- 新增员工 client、契约和视图模型，统一员工/可绑定账号读取、员工保存和账号状态展示。
- 员工协调页从 263 行降至 214 行且不再直接调用 `fetch`；根级存量页面从 4 个降至 3 个。
- 扩展员工守卫，同时验证模块边界和员工业务不变式。

## 影响范围

- 保留员工列表/卡片、精确搜索、排序、账号可选绑定和停用交互。
- 账号绑定仍不会修改角色、审核状态或权限；不修改 Prisma Schema、API 路径或服务器正式数据。

## 验证结果

- 员工、配置模块、页面模块和模块边界专项校验通过。
- TypeScript 类型检查与 Next.js 生产构建通过。

## 数据安全

- 本机 SQLite 中只保留一条明确标记的测试记录；数据库文件和构建缓存不纳入提交。
- 正式数据仍以服务器数据库为准，本次没有连接、读取或写入服务器正式库。
