# MES-lite v0.1.195

发布日期：2026-08-05

## 更新内容

- 将可配置的 AI 助手图标接入统一页面加载指示器，等待时显示旋转和呼吸动效。
- 首次刷新鉴权、Next.js 页面等待、功能页懒加载和仪表盘初始化统一使用新指示器。
- 加载图标会读取“应用到系统”后的参数，并继续遵循用户的减少动态效果偏好。

## 影响范围

- 仅调整页面级等待状态，不改变鉴权、业务请求或页面加载顺序。
- 不为展示动画人为延长加载时间；页面已缓存或本地请求很快时，指示器可能只短暂出现。
- 本版本仍位于 `feat/ai-mark-runtime-config` 功能分支，未改动主分支。

## 验证结果

- 指定文件 ESLint 检查通过，仅保留 `app/page.tsx` 中既有 Hook 依赖警告。
- `npm run verify:ai-mark` 通过。
- `npm run build` 通过，页面等待组件与动态功能页编译正常。
