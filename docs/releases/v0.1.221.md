# MES-lite v0.1.221

发布日期：2026-08-07

## 更新内容

- 在 BOM 设置页的关键词搜索旁补齐“高级搜索”入口。
- 支持按产出物料、投入物料、BOM 名称、版本、用途、启用状态和默认方案组合搜索。
- BOM 关键词搜索升级为空格分隔的多关键词 AND 匹配。
- BOM 精确条件可以与关键词一起保存为条件类快捷搜索，并可直接恢复。

## 影响范围

- 只调整 BOM 工作区的前端搜索与结果过滤，不修改数据库结构和 BOM 数据。

## 验证结果

- TypeScript 与公共搜索回归校验通过。
- Next.js 生产构建通过。
