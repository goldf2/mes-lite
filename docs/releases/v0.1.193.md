# MES-lite v0.1.193

发布日期：2026-08-05

## 更新内容

- 新增统一的 MES-lite 页面等待组件，使用 002 AI 助手图标进行缓慢旋转和轻微呼吸。
- 首次刷新鉴权、Next.js 路由加载、动态功能页加载和仪表盘初始化统一使用新等待状态。
- 保留按钮提交、PDF 渲染等局部操作原有的小型进度提示，避免遮挡业务内容。

## 影响范围

- 仅调整页面级加载反馈，不改变鉴权流程、业务请求顺序或数据处理逻辑。
- 等待图标遵循系统的“减少动态效果”偏好，用户启用后自动停止动画。

## 验证结果

- 指定文件 ESLint 检查通过，仅保留 `app/page.tsx` 既有 Hook 依赖警告。
- `npm run build` 通过，类型检查、静态页面生成和生产构建正常。
